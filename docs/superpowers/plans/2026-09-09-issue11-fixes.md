# 修正事項-20260910（issue #11）対応 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** issue #11 の12項目（表記の漢字化、配置の自由化、必殺技の発動方法、PC での座標ずれ、下パネルの再編、敵の速度と時間湧きと索敵非表示、レベルアップの緩和とレベル表示）をすべて実装する。

**Architecture:** 既存の層構造を崩さない。`src/core` と `src/engine` は DOM を参照しない純ロジックのまま、調整値は `assets/*.json` へ出す。UI は「下パネル上段 = `MESSAGE_BAR`（配置中は『はじめる』、戦闘中はセリフ欄）／下段 = ポートレート（選択＋必殺技発動）」という新しい役割分担に組み替える。canvas のサイズ決定は CSS と JS の二重管理をやめ、`#stage` の実寸を測る JS 一本に統一する。

**Tech Stack:** TypeScript, Vite, Vitest, Canvas2D（既存スタック。新規ライブラリなし）

**Spec:** `docs/superpowers/specs/2026-09-09-issue11-fixes-design.md`

## Global Constraints

- `src/core/**` と `src/engine/**` は `window` / `document` / `localStorage` を参照しない（README.md の既存ルール）
- Canvas2D の描画コード（`src/render/draw.ts`、`src/ui/screens.ts`）にはユニットテストを書かない。テストは純ロジックとレイアウト計算に寄せる（CLAUDE.md）
- データはパラメータ（JSON）、振る舞いはコード、という既存の線引きを崩さない
- コミットメッセージは Conventional Commits + 日本語の要約。要約は漢字仮名交じりで書く（CLAUDE.md）
- ゲーム内 UI の表示文字列は、**Task 11 までは総ひらがなのまま触らない**。Task 11 で一括して漢字仮名交じりへ変える。先に変えると以降のレイアウト変更で同じ文字列を二度書き直すことになる
- 論理解像度は 540×945、マップは 16列 × 23行、`cell` は 32（変更しない）
- `npm test` と `npm run build` が通ることを各タスクの完了条件とする

## 仕様からの具体化（計画時に決めた詳細）

spec が型レベルまで決めていなかった点を、実装前にここで確定させる。

- `placement.starts` の要素は `{ pos: Vec2 }` ではなく **`Vec2` を直接並べる**。旧 `placementZone` は円の中心を表すため `pos` で包んでいたが、`starts` は座標そのものなので包む必要がない
- `StageDef.spawners` は `?` を付けず **常に配列**にする（JSON に書かなければ `[]`）。`?.` や `?? []` が呼び出し側に散るのを避ける
- `alertMarks` は `{ pos, defId }` を返す。頭上の「！」の高さを `drawHalf` で出すために defId が要る
- `alertMarks` は **`aggressive` の敵を除く**。aggressive は開始時から追ってくるので常に「！」が出てしまい、「気づかれた」サインとして意味をなさない
- `growth.json` の `hitXp` / `healXp` / `clearXp` は**整数で検証する**。経験値が小数になるとリザルト画面に `4.5/12` のような値が出る。`assistRatio` は 0〜1 の小数を許し、掛けた結果に `Math.floor` を使う
- `registry.ts` は `growth.json` が無い場合にエラーを積む。型を満たすための仮値は置くが、エラーが積まれた時点でゲームは起動しないので実際には使われない（`schema.ts` のヘルパと同じ流儀）

---

### Task 1: canvas のサイズ決定を一本化する（項目4）

**Files:**
- Modify: `src/render/viewport.ts`
- Modify: `src/render/viewport.test.ts`
- Modify: `index.html`
- Modify: `src/main.ts:39-52`（`resize()` と `window` への購読）

**Interfaces:**
- Consumes: `LOGICAL_W` / `LOGICAL_H`（既存、`src/render/viewport.ts`）
- Produces: `fitCanvas(boxW: number, boxH: number, dpr: number): CanvasFit`、`type CanvasFit = { cssW: number; cssH: number; pixelW: number; pixelH: number }`

- [ ] **Step 1: 失敗するテストを書く**

`src/render/viewport.test.ts` の末尾に追記する。先頭の import に `fitCanvas` を足す。

```ts
describe('fitCanvas', () => {
  it('縦に余裕があるときは幅が決め手になる', () => {
    expect(fitCanvas(540, 2000, 1)).toEqual({ cssW: 540, cssH: 945, pixelW: 540, pixelH: 945 });
  });

  it('横に余裕があるときは高さが決め手になる', () => {
    expect(fitCanvas(2000, 945, 1)).toEqual({ cssW: 540, cssH: 945, pixelW: 540, pixelH: 945 });
  });

  it('拡大率が1を超えても縦横比を保つ', () => {
    // 1920x1080 は高さが決め手。945 -> 1080 の 1.1428 倍
    const fit = fitCanvas(1920, 1080, 1);
    expect(fit.cssH).toBe(1080);
    expect(fit.cssW).toBe(617);
    // 540/945 = 0.5714。1px 未満のずれだけに収まること
    expect(Math.abs(fit.cssW / fit.cssH - LOGICAL_W / LOGICAL_H)).toBeLessThan(0.001);
  });

  it('backing store は dpr 倍の整数になる', () => {
    expect(fitCanvas(540, 945, 1.5)).toEqual({ cssW: 540, cssH: 945, pixelW: 810, pixelH: 1418 });
  });

  it('箱がつぶれていても 1px 未満にしない', () => {
    const fit = fitCanvas(0, 0, 1);
    expect(fit.pixelW).toBeGreaterThanOrEqual(1);
    expect(fit.pixelH).toBeGreaterThanOrEqual(1);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/render/viewport.test.ts`
Expected: FAIL。`fitCanvas` が export されていないため import エラーになる

- [ ] **Step 3: `fitCanvas` を実装する**

`src/render/viewport.ts` の `computeViewport` の下に足す。

```ts
/** canvas の CSS 寸法と backing store 寸法。CSS 側とJS 側が別々に寸法を決めないための1本 */
export type CanvasFit = { cssW: number; cssH: number; pixelW: number; pixelH: number };

/**
 * 与えられた箱に論理解像度の縦横比で収まる canvas の寸法を返す。
 * cssW / cssH を整数に丸めるので縦横比は 1px 未満ずれる。そのぶんは
 * computeViewport がレターボックスとして吸収する
 */
export function fitCanvas(boxW: number, boxH: number, dpr: number): CanvasFit {
  const scale = Math.min(boxW / LOGICAL_W, boxH / LOGICAL_H);
  const cssW = Math.floor(LOGICAL_W * scale);
  const cssH = Math.floor(LOGICAL_H * scale);
  return {
    cssW,
    cssH,
    pixelW: Math.max(1, Math.round(cssW * dpr)),
    pixelH: Math.max(1, Math.round(cssH * dpr)),
  };
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npx vitest run src/render/viewport.test.ts`
Expected: PASS

- [ ] **Step 5: `index.html` から寸法の制約を外す**

`<style>` と `<body>` を次に差し替える。`#game` から `width` / `max-width` / `max-height` / `aspect-ratio` を外すのが要点。`max-width: 540px` がインライン style の幅を頭打ちにして、横に潰れた描画と入力座標の食い違いを起こしていた。

```html
    <style>
      html, body {
        width: 100%;
        height: 100%;
        margin: 0;
        background: #071428;
        overflow: hidden;
      }

      /* safe-area の内側を占める箱。canvas の寸法はこの箱の実寸から JS が決める */
      #stage {
        position: fixed;
        top: env(safe-area-inset-top);
        right: env(safe-area-inset-right);
        bottom: env(safe-area-inset-bottom);
        left: env(safe-area-inset-left);
        display: grid;
        place-items: center;
      }

      #game {
        display: block;
        touch-action: none;
        image-rendering: pixelated;
      }
    </style>
  </head>
  <body>
    <div id="stage"><canvas id="game"></canvas></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
```

- [ ] **Step 6: `main.ts` の `resize()` を書き換える**

`src/main.ts:39-52` を次に差し替える。

```ts
const canvas = document.getElementById('game') as HTMLCanvasElement;
const stageBox = document.getElementById('stage') as HTMLElement;
const ctx = canvas.getContext('2d')!;

function resize(): void {
  const box = stageBox.getBoundingClientRect();
  const fit = fitCanvas(box.width, box.height, window.devicePixelRatio);
  canvas.style.width = `${fit.cssW}px`;
  canvas.style.height = `${fit.cssH}px`;
  // 当てたあとの実寸から backing store を決める。CSS が何らかの制約を効かせても、
  // 描画と入力が同じ実寸を見ているかぎりクリック位置はずれない
  const actual = canvas.getBoundingClientRect();
  canvas.width = Math.max(1, Math.round(actual.width * window.devicePixelRatio));
  canvas.height = Math.max(1, Math.round(actual.height * window.devicePixelRatio));
  // width への代入でコンテキストの状態が全部リセットされるので、ここで毎回入れ直す
  ctx.imageSmoothingEnabled = false;
}
// window の resize ではブラウザズームによる devicePixelRatio の変化を拾えない
new ResizeObserver(resize).observe(stageBox);
resize();
```

`import` 行の `computeViewport, logicalToMap, ...` に `fitCanvas` を足す。`window.addEventListener('resize', resize)` の行は削除する。

- [ ] **Step 7: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功

- [ ] **Step 8: コミット**

```bash
git add src/render/viewport.ts src/render/viewport.test.ts index.html src/main.ts
git commit -m "fix: canvasの寸法決定をJS一本に統一してPCでのクリック位置ずれを直す"
```

---

### Task 2: 配置を領域方式に変える（項目2）

**Files:**
- Modify: `src/engine/schema.ts`（`StageDef`、`validateStageDef`）
- Modify: `src/engine/schema.test.ts`
- Modify: `assets/stages/stage1.json`、`assets/stages/stage2.json`、`assets/stages/stage3.json`
- Modify: `src/core/state.ts`（`PLACEMENT_RADIUS` 廃止、`canPlaceAt`、`placeUnit`、`createBattleState`）
- Modify: `src/core/state.test.ts`
- Modify: `src/ui/screens.ts`（`drawPlacement`）
- Modify: `src/main.ts`（ドラッグプレビューの `blocked`）
- Modify: `src/render/objectives-view.test.ts:6`（`BASE` の `placementZone` を `placement` へ）

**Interfaces:**
- Consumes: `isWalkableAt(grid, pos)`（既存、`src/core/field.ts`）
- Produces: `type PlacementDef = { minY: number; starts: Vec2[] }`、`StageDef.placement: PlacementDef`、`canPlaceAt(stage: StageDef, grid: Grid, pos: Vec2): boolean`

- [ ] **Step 1: 失敗するテストを書く**

`src/core/state.test.ts` の `describe('placeUnit')` を次に差し替える。先頭 import の `PLACEMENT_RADIUS` を `canPlaceAt` に替える。

```ts
describe('canPlaceAt', () => {
  it('境界の線より下なら置ける', () => {
    const { stage, state } = fresh();
    expect(canPlaceAt(stage, state.grid, { x: 240, y: stage.placement.minY })).toBe(true);
  });

  it('境界の線より上は置けない', () => {
    const { stage, state } = fresh();
    expect(canPlaceAt(stage, state.grid, { x: 240, y: stage.placement.minY - 1 })).toBe(false);
  });

  it('歩けないマスは置けない', () => {
    const { stage, state } = fresh();
    expect(canPlaceAt(stage, state.grid, { x: 0, y: 0 })).toBe(false);
  });
});

describe('placeUnit', () => {
  it('境界の線より下なら置ける', () => {
    const { stage, state } = fresh();
    const dest = { x: 400, y: stage.placement.minY + 32 };
    const uid = unitOf(state, 'roran').uid;
    expect(placeUnit(state, uid, dest)).toBe(true);
    expect(unitOf(state, 'roran').pos).toEqual(dest);
  });

  it('境界の線より上には置けない', () => {
    const { stage, state } = fresh();
    const uid = unitOf(state, 'roran').uid;
    const before = { ...unitOf(state, 'roran').pos };
    expect(placeUnit(state, uid, { x: 240, y: stage.placement.minY - 32 })).toBe(false);
    expect(unitOf(state, 'roran').pos).toEqual(before);
  });

  it('歩けないマスには置けない', () => {
    const { state } = fresh();
    const uid = unitOf(state, 'roran').uid;
    expect(placeUnit(state, uid, { x: 0, y: 0 })).toBe(false);
  });

  it('知らない uid なら false', () => {
    const { state } = fresh();
    expect(placeUnit(state, 'yuurei', { x: 112, y: 600 })).toBe(false);
  });
});
```

`describe('createBattleState: ステージからの はいち')` の「味方を placementZone の うえに おく」を次に差し替える。

```ts
  it('味方を placement.starts の座標に置く', () => {
    const { stage, state } = fresh();
    const players = state.units.filter((u) => u.side === 'player');
    players.forEach((u, i) => {
      expect(u.pos).toEqual(stage.placement.starts[i % stage.placement.starts.length]);
    });
  });
```

同じファイルの「味方の初期配置地点への フローフィールドを キャッシュから ひける」の `stage.placementZone[0]!.pos` を `stage.placement.starts[0]!` に直す。

`src/engine/schema.test.ts` に追記する。既存のテストが使っているステージ JSON の組み立て方に合わせること（最小の正常データを作り、1フィールドだけ壊して検証する形）。

```ts
describe('validateStageDef: placement', () => {
  it('minY と starts を読む', () => {
    const r = validateStageDef('assets/stages/t.json', stageRaw());
    expect(r.ok).toBe(true);
    if (r.ok) expect(r.value.placement).toEqual({ minY: 32, starts: [{ x: 48, y: 48 }] });
  });

  it('starts が minY より上だとエラー', () => {
    const raw = stageRaw({ placement: { minY: 48, starts: [{ x: 48, y: 32 }] } });
    const r = validateStageDef('assets/stages/t.json', raw);
    expect(r.ok).toBe(false);
    if (!r.ok) expect(r.errors.some((e) => e.path === 'placement.starts[0]')).toBe(true);
  });

  it('starts が空だとエラー', () => {
    const r = validateStageDef('assets/stages/t.json', stageRaw({ placement: { minY: 32, starts: [] } }));
    expect(r.ok).toBe(false);
  });

  it('placement が無いとエラー', () => {
    const raw = stageRaw();
    delete (raw as Record<string, unknown>).placement;
    expect(validateStageDef('assets/stages/t.json', raw).ok).toBe(false);
  });
});
```

`stageRaw(over?)` は、このファイルに既存のステージ生成ヘルパがあればそれを使い、無ければ次を足す。

```ts
/** 検証を通る最小のステージ。引数で1フィールドだけ差し替える */
function stageRaw(over: Record<string, unknown> = {}): Record<string, unknown> {
  return {
    id: 't', order: 10, name: 'T', cell: 32,
    mapRows: ['###', '#.#', '#.#', '###'],
    placement: { minY: 32, starts: [{ x: 48, y: 48 }] },
    roster: ['roran'],
    enemies: [],
    victory: { type: 'reach', pos: { x: 48, y: 48 }, radius: 10, by: 'any' },
    defeat: [{ type: 'allPlayerUnitsLost' }],
    ...over,
  };
}
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/core/state.test.ts src/engine/schema.test.ts`
Expected: FAIL。`canPlaceAt` が無く、`stage.placement` も存在しない

- [ ] **Step 3: スキーマを変える**

`src/engine/schema.ts` の `StageDef` の `placementZone` 行を差し替える。

```ts
export type PlacementDef = {
  /** この y 以上（画面で下）なら配置できる */
  minY: number;
  /** ステージ開始時の味方の初期位置。roster より少なければ先頭から繰り返す */
  starts: Vec2[];
};
```

```ts
export type StageDef = {
  id: string;
  order: number;
  name: string;
  cell: number;
  mapRows: string[];
  placement: PlacementDef;
  roster: string[];
  enemies: EnemyPlacement[];
  victory: VictoryCond;
  defeat: DefeatCond[];
  intro?: IntroLine[];
  outro?: IntroLine[];
};
```

`readIntroLine` の下に読み取り関数を足す。

```ts
function readPlacement(
  ctx: Ctx,
  v: unknown,
  mapRows: string[],
  cell: number,
  checkWalkable: (path: string, pos: Vec2) => void,
): PlacementDef {
  const o = requireObject(ctx, 'placement', v);
  if (!o) return { minY: 0, starts: [] };

  const maxY = mapRows.length * cell;
  const minY = requireNumber(
    ctx, 'placement.minY', o.minY, maxY > 0 ? { min: 0, max: maxY - 1 } : { min: 0 },
  ) ?? 0;

  const raw = requireArray(ctx, 'placement.starts', o.starts, { min: 1 }) ?? [];
  const starts: Vec2[] = [];
  raw.forEach((item, i) => {
    const path = `placement.starts[${i}]`;
    const pos = requireVec2(ctx, path, item);
    if (pos === null) return;
    checkWalkable(path, pos);
    if (pos.y < minY) fail(ctx, path, `minY（${minY}）いじょうで ないと いけない`);
    starts.push(pos);
  });

  return { minY, starts };
}
```

`validateStageDef` の `zoneRaw` / `placementZone` を組み立てている 5行を削除し、`stage` の組み立てを次にする。

```ts
  const stage: StageDef = {
    id: requireString(ctx, 'id', o.id) ?? '',
    order: requireNumber(ctx, 'order', o.order, { min: 1, int: true }) ?? 1,
    name: requireString(ctx, 'name', o.name) ?? '',
    cell,
    mapRows,
    placement: readPlacement(ctx, o.placement, mapRows, cell, checkWalkable),
    roster: readStringArray(ctx, 'roster', o.roster, 1),
    enemies,
    victory: readVictory(ctx, o.victory),
    defeat: readDefeat(ctx, o.defeat),
  };
```

- [ ] **Step 4: ステージ JSON を直す**

3ファイルそれぞれの `placementZone` ブロックを次に差し替える（座標は3ステージとも同じ）。

```json
  "placement": {
    "minY": 528,
    "starts": [
      { "x": 144, "y": 656 },
      { "x": 240, "y": 656 },
      { "x": 144, "y": 592 },
      { "x": 240, "y": 592 }
    ]
  },
```

`minY` の 528 は、旧配置円（中心 y 592・656、半径 64）が覆っていた範囲の上端。

- [ ] **Step 5: `canPlaceAt` と `placeUnit` を実装する**

`src/core/state.ts` の先頭の定数から `PLACEMENT_RADIUS` を削除し、`import { distance, isWalkableAt, makeGrid }` の `distance` を外す（他で使っていない）。

```ts
const HP_PER_LEVEL = 3;
const POWER_PER_LEVEL = 1;
```
はそのまま（Task 9 で差し替える）。

`createBattleState` の roster 生成を次にする。

```ts
  const roster = stage.roster.map((defId, i) => {
    const def = reg.units.get(defId);
    if (!def) throw new Error(`roster に しらない ユニット: ${defId}`);
    const start = stage.placement.starts[i % stage.placement.starts.length]!;
    return makeUnit({
      uid: `p${i + 1}`, def, side: 'player', controller: 'player', pos: start,
      level: progress[defId]?.level ?? 1, xp: progress[defId]?.xp ?? 0, ai: null,
    });
  });
```

`placeUnit` の上に `canPlaceAt` を置き、`placeUnit` 本体を書き換える。

```ts
/**
 * そこへ配置できるか。配置の実行とドラッグプレビューの赤表示が必ずこの1本を通る。
 * 別々に書くと「プレビューは置けそうに見えるのに離すと失敗する」がすぐ起きる
 */
export function canPlaceAt(stage: StageDef, grid: Grid, pos: Vec2): boolean {
  if (!isWalkableAt(grid, pos)) return false;
  return pos.y >= stage.placement.minY;
}

export function placeUnit(state: BattleState, uid: string, pos: Vec2): boolean {
  if (!canPlaceAt(state.stage, state.grid, pos)) return false;
  const unit = state.units.find((u) => u.uid === uid && u.side === 'player');
  if (!unit) return false;
  unit.pos = { ...pos };
  unit.goalField = null;
  unit.goalPos = null;
  return true;
}
```

`import type` に `Grid` を足す（`./types` から）。

- [ ] **Step 6: 配置フェーズの描画を変える**

`src/ui/screens.ts` の `drawPlacement` を次に差し替える。import から `PLACEMENT_RADIUS` を外す。

```ts
export function drawPlacement(ctx: CanvasRenderingContext2D, state: BattleState): void {
  ctx.fillStyle = 'rgba(16, 24, 32, 0.35)';
  ctx.fillRect(0, 0, LOGICAL_W, LOGICAL_H);

  // 置ける範囲を見せる。境界の線より下が置ける側
  const edgeY = mapToLogical({ x: 0, y: state.stage.placement.minY }).y;
  ctx.fillStyle = 'rgba(255, 212, 121, 0.12)';
  ctx.fillRect(0, edgeY, LOGICAL_W, BOTTOM_PANEL_Y - edgeY);
  ctx.strokeStyle = '#ffd479';
  ctx.lineWidth = 2;
  ctx.setLineDash([8, 6]);
  ctx.beginPath();
  ctx.moveTo(0, edgeY);
  ctx.lineTo(LOGICAL_W, edgeY);
  ctx.stroke();
  ctx.setLineDash([]);

  ctx.fillStyle = INK;
  ctx.font = '20px sans-serif';
  ctx.fillText('きいろい せんより したに なかまを おこう', 24, 760);
  button(ctx, SKILL_BUTTON, 'はじめる');
}
```

- [ ] **Step 7: ドラッグプレビューを同じ判定に通す**

`src/main.ts` の `render()` 末尾の `blocked` を次にする。

```ts
    const blocked = phase === 'placement'
      ? !canPlaceAt(battle.stage, battle.grid, dragMap)
      : !isWalkableAt(battle.grid, dragMap);
```

import を `import { canPlaceAt, beginBattle, createBattleState, placeUnit } from './core/state';` のように調整する（既存の import 順に合わせる）。

- [ ] **Step 8: テストのキャスト用データを直す**

`src/render/objectives-view.test.ts:6` の `BASE` の `placementZone: []` を `placement: { minY: 0, starts: [] }` に直す。

- [ ] **Step 9: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功

- [ ] **Step 10: コミット**

```bash
git add src/engine/schema.ts src/engine/schema.test.ts assets/stages src/core/state.ts src/core/state.test.ts src/ui/screens.ts src/main.ts src/render/objectives-view.test.ts
git commit -m "feat: 配置を円の枠から境界線より下の自由配置に変える"
```

---

### Task 3: 下パネル上段の枠と主要ボタンの配色、レベル表示（項目3・12）

**Files:**
- Modify: `src/ui/layout.ts`（`SKILL_BUTTON` → `MESSAGE_BAR`）
- Modify: `src/ui/layout.test.ts`
- Modify: `src/ui/screens.ts`（`button()` に primary、`drawBottomBar` にレベル）
- Modify: `src/main.ts`（`SKILL_BUTTON` の参照2か所）

**Interfaces:**
- Consumes: `portraitSlot(i)`、`BOTTOM_PANEL_Y`（既存、`src/ui/layout.ts`）
- Produces: `MESSAGE_BAR: Rect`（`{ x: 8, y: 788, w: 524, h: 64 }`）

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/layout.test.ts` に追記する。import の `SKILL_BUTTON` を `MESSAGE_BAR` に替える（既存の `SKILL_BUTTON` を参照しているテストがあれば名前だけ差し替える）。

```ts
describe('MESSAGE_BAR', () => {
  it('下パネルの中にある', () => {
    expect(MESSAGE_BAR.y).toBeGreaterThanOrEqual(BOTTOM_PANEL_Y);
    expect(MESSAGE_BAR.y + MESSAGE_BAR.h).toBeLessThanOrEqual(LOGICAL_H);
  });

  it('ポートレートと重ならない', () => {
    expect(MESSAGE_BAR.y + MESSAGE_BAR.h).toBeLessThanOrEqual(portraitSlot(0).y);
  });

  it('画面の横幅に収まる', () => {
    expect(MESSAGE_BAR.x).toBeGreaterThanOrEqual(0);
    expect(MESSAGE_BAR.x + MESSAGE_BAR.w).toBeLessThanOrEqual(LOGICAL_W);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/ui/layout.test.ts`
Expected: FAIL。`MESSAGE_BAR` が export されていない

- [ ] **Step 3: `MESSAGE_BAR` に置き換える**

`src/ui/layout.ts` の `SKILL_BUTTON` を次に差し替える。

```ts
/**
 * 下パネルの上段。配置フェーズは「はじめる」ボタン、戦闘中はセリフ欄として使う。
 * 押す場所・読む場所がフェーズで動かないほうが覚えやすい
 */
export const MESSAGE_BAR: Rect = { x: 8, y: 788, w: 524, h: 64 };
```

`src/main.ts` の `hitRect(SKILL_BUTTON, p)` 2か所（配置フェーズと戦闘フェーズ）と import を `MESSAGE_BAR` に直す。`src/ui/screens.ts` の `drawPlacement` / `drawSkillButton` の `SKILL_BUTTON` も同様に直す。

- [ ] **Step 4: テストが通ることを確認する**

Run: `npx vitest run src/ui/layout.test.ts`
Expected: PASS

- [ ] **Step 5: `button()` に primary 配色を足す**

`src/ui/screens.ts` の `button` を次に差し替える。

```ts
/**
 * primary は主要な導線のボタン。有効な暗い紺（#2c4a63）と無効な暗い灰（#2a2f35）は
 * 並べないと区別がつかず、「押せないボタン」に見えてしまうため、押してほしいボタンは
 * アクセント色で塗る
 */
function button(
  ctx: CanvasRenderingContext2D,
  r: Rect,
  label: string,
  enabled = true,
  primary = false,
): void {
  const fill = !enabled ? '#2a2f35' : primary ? '#ffd479' : '#2c4a63';
  panel(ctx, r, fill);
  ctx.fillStyle = !enabled ? '#78808a' : primary ? '#1a1a1a' : INK;
  ctx.font = '26px sans-serif';
  ctx.textAlign = 'center';
  ctx.textBaseline = 'middle';
  ctx.fillText(label, r.x + r.w / 2, r.y + r.h / 2);
  ctx.textAlign = 'left';
  ctx.textBaseline = 'alphabetic';
}
```

呼び出し側を直す。

- `drawTitle`: `button(ctx, BTN.titleNew, 'はじめから', true, true);`
- `drawPlacement`: `button(ctx, MESSAGE_BAR, 'はじめる', true, true);`
- `drawResult`: `button(ctx, BTN.next, 'つぎへ', true, true);`
- `drawDefeat`: `button(ctx, BTN.retry, 'もういちど', true, true);`

他（`titleContinue`、`toSelect`、`skip`）は変更しない。

- [ ] **Step 6: ポートレートにレベルを出す**

`src/ui/screens.ts` の `drawBottomBar` の中、`ctx.fillText(def.name, r.x + 42, r.y + 28);` の直後に足す。

```ts
      // レベルは枠の右上。名前の右は roleBadgeIn と重なる
      ctx.font = '14px sans-serif';
      ctx.textAlign = 'right';
      ctx.fillText(`Lv${unit.level}`, r.x + r.w - 8, r.y + 18);
      ctx.textAlign = 'left';
      ctx.font = '18px sans-serif';
```

- [ ] **Step 7: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功

- [ ] **Step 8: コミット**

```bash
git add src/ui/layout.ts src/ui/layout.test.ts src/ui/screens.ts src/main.ts
git commit -m "feat: 主要ボタンをアクセント色にし、ポートレートにレベルを出す"
```

---

### Task 4: ポートレートのタップで必殺技を出す（項目5）

**Files:**
- Delete: `src/ui/skillbutton.ts`、`src/ui/skillbutton.test.ts`
- Modify: `src/ui/screens.ts`（`drawSkillButton` 削除、ポートレートの発動可能サイン）
- Modify: `src/main.ts`（`beginMapPointer` のポートレート分岐、戦闘フェーズのスキル分岐削除、`render()` の呼び出し削除）

**Interfaces:**
- Consumes: `skillParam(reg, skillId, key, fallback)`（既存、`src/engine/registry.ts`）、`MESSAGE_BAR`（Task 3）、`BOTTOM_PANEL_Y`（既存）
- Produces: なし（既存関数の挙動変更のみ）

- [ ] **Step 1: ポートレートの分岐を書き換える**

`src/main.ts` の `beginMapPointer` の先頭、ポートレートのループを次に差し替える。

```ts
function beginMapPointer(state: BattleState, p: Vec2, ev: PointerEvent): void {
  if (pointerStart !== null) return; // 別の指のジェスチャが進行中は新しいジェスチャを始めない
  // drawBottomBar と同じ無フィルタ配列でインデックスを解決する。playerUnits() は retired を
  // 除外して再インデックスするため、これと混ぜるとポートレートの見た目とタップ対象がずれる
  const portraitUnits = state.units.filter((u) => u.side === 'player').slice(0, 4);
  for (let i = 0; i < 4; i++) {
    if (!hitRect(portraitSlot(i), p)) continue;
    pointerStart = null;
    const unit = portraitUnits[i];
    if (!unit || unit.retired) return;
    // ポートレートのタップは「選択して必殺技を出す」。選択の解除はマップ上の再タップで行う
    selected = unit.uid;
    if (phase !== 'battle') return;
    if (unit.skillId === null || state.time < unit.skillCooldownUntil) return;
    if (skillParam(state.reg, unit.skillId, 'needsDest', 0) === 1) pendingSkill = unit.uid;
    else commands.push({ type: 'skill', uid: unit.uid });
    return;
  }
  // 下パネルのタップはマップ操作に落とさない。マップの外を移動先に解釈させない
  if (p.y >= BOTTOM_PANEL_Y) {
    pointerStart = null;
    return;
  }
  const startMap = logicalToMap(p);
  ...
```

以降（`const startMap` から）は現状のまま。ただし `bubbleUid` の行は Task 5 で消すのでここでは触らない。

import に `BOTTOM_PANEL_Y` を足す（`./ui/layout` から）。

- [ ] **Step 2: 戦闘フェーズのスキルボタン分岐を消す**

`src/main.ts` の `onPointerDown` の `case 'battle':` を次に差し替える。

```ts
    case 'battle': {
      if (!battle) return;
      if (pendingSkill) {
        pointerStart = null;
        commands.push({ type: 'skill', uid: pendingSkill, dest: logicalToMap(p) });
        pendingSkill = null;
        return;
      }
      beginMapPointer(battle, p, ev);
      return;
    }
```

`case 'placement':` の `MESSAGE_BAR` 分岐（「はじめる」）はそのまま残す。

- [ ] **Step 3: スキルボタンの描画を消す**

`src/ui/screens.ts` から `drawSkillButton` 関数と `import { skillButtonState } from './skillbutton';` を削除する。`src/main.ts` の `render()` の `drawSkillButton(ctx, registry, battle, selected);` の行と import を削除する。

- [ ] **Step 4: 発動できるポートレートに縁を出す**

`src/ui/screens.ts` の `drawBottomBar` のクールダウンゲージを描くブロックを次に差し替える。

```ts
      if (unit.skillId !== null && !unit.retired) {
        const total = skillParam(reg, unit.skillId, 'cooldown', DEFAULT_SKILL_COOLDOWN);
        const remaining = Math.max(0, unit.skillCooldownUntil - state.time);
        const ratio = total > 0 ? 1 - remaining / total : 1;
        ctx.fillStyle = '#000';
        ctx.fillRect(r.x + 8, r.y + 70, 113, 5);
        ctx.fillStyle = '#ffd479';
        ctx.fillRect(r.x + 8, r.y + 70, 113 * Math.max(0, Math.min(1, ratio)), 5);

        // 押せば技が出る状態を縁で示す。押せない理由を文字で出す代わり
        if (remaining <= 0) {
          ctx.strokeStyle = '#ffd479';
          ctx.lineWidth = 2;
          ctx.strokeRect(r.x + 2, r.y + 2, r.w - 4, r.h - 4);
        }
      }
```

- [ ] **Step 5: スキルボタンのモジュールを削除する**

```bash
git rm src/ui/skillbutton.ts src/ui/skillbutton.test.ts
```

- [ ] **Step 6: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功。`skillbutton` への参照が残っていればビルドが落ちるので、落ちたら残りの参照を消す

- [ ] **Step 7: コミット**

```bash
git add -A src/main.ts src/ui/screens.ts src/ui/skillbutton.ts src/ui/skillbutton.test.ts
git commit -m "feat: ポートレートのタップで必殺技を発動し、スキルボタンを廃止する"
```

---

### Task 5: セリフを頭上の吹き出しから下パネルへ移す（項目6・7）

**Files:**
- Create: `src/ui/speech.ts`、`src/ui/speech.test.ts`
- Delete: `src/ui/bubbles.ts`、`src/ui/bubbles.test.ts`
- Modify: `src/ui/layout.ts`（`BUBBLE_*` と `bubbleRectAt` / `bubbleLines` 削除、`SPEECH_*` と `speechLines` 追加）
- Modify: `src/ui/layout.test.ts`（吹き出しのテストを削除し、`speechLines` のテストを追加）
- Modify: `src/ui/input.ts`（`bubbleUid` と `dismissBubble` 削除）
- Modify: `src/ui/input.test.ts`
- Modify: `src/ui/screens.ts`（`drawBubble` → `drawSpeechBar`）
- Modify: `src/main.ts`（`bubbleAt` 削除、配線の差し替え）

**Interfaces:**
- Consumes: `DialogueRequest`（既存、`src/core/dialogue.ts`）、`wrapText`（既存、`src/ui/talk.ts`）、`MESSAGE_BAR`（Task 3）、`drawFace`（既存、`src/render/sprites.ts`）
- Produces: `SPEECH_DURATION`、`type Speech = { uid: string; defId: string; text: string; ttl: number }`、`type SpeechState = { current: Speech | null }`、`makeSpeechState()`、`pushSpeech(state, reqs)`、`tickSpeech(state, dt)`、`clearSpeech(state)`、`speechLines(text): string[]`、`drawSpeechBar(ctx, reg, speech, images)`

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/speech.test.ts` を新規作成する。

```ts
import { describe, expect, it } from 'vitest';
import { SPEECH_DURATION, clearSpeech, makeSpeechState, pushSpeech, tickSpeech } from './speech';
import type { DialogueRequest } from '../core/dialogue';

const req = (uid: string, defId: string, text: string): DialogueRequest =>
  ({ uid, speaker: { side: 'ally', id: defId }, lineId: text, text });

describe('SpeechState', () => {
  it('最初は空', () => {
    expect(makeSpeechState().current).toBeNull();
  });

  it('積むと話者と本文が入る', () => {
    const s = makeSpeechState();
    pushSpeech(s, [req('p1', 'roran', 'ここは とおさない！')]);
    expect(s.current).toEqual({
      uid: 'p1', defId: 'roran', text: 'ここは とおさない！', ttl: SPEECH_DURATION,
    });
  });

  it('同じ tick に複数来たら先頭だけを採る', () => {
    const s = makeSpeechState();
    // pickDialogue は優先順位の昇順で返す。末尾で上書きすると優先度の低い方が勝つ
    pushSpeech(s, [req('p1', 'roran', 'さきに でる'), req('p2', 'gau', 'あとの やつ')]);
    expect(s.current!.text).toBe('さきに でる');
  });

  it('空の配列では何も変わらない', () => {
    const s = makeSpeechState();
    pushSpeech(s, [req('p1', 'roran', 'あ')]);
    pushSpeech(s, []);
    expect(s.current!.text).toBe('あ');
  });

  it('新しい発話で上書きし、寿命も引き直す', () => {
    const s = makeSpeechState();
    pushSpeech(s, [req('p1', 'roran', 'ふるい')]);
    tickSpeech(s, 1);
    pushSpeech(s, [req('p2', 'gau', 'あたらしい')]);
    expect(s.current!.text).toBe('あたらしい');
    expect(s.current!.ttl).toBe(SPEECH_DURATION);
  });

  it('寿命が尽きたら消える', () => {
    const s = makeSpeechState();
    pushSpeech(s, [req('p1', 'roran', 'あ')]);
    tickSpeech(s, SPEECH_DURATION - 0.1);
    expect(s.current).not.toBeNull();
    tickSpeech(s, 0.2);
    expect(s.current).toBeNull();
  });

  it('空を tick しても壊れない', () => {
    expect(() => tickSpeech(makeSpeechState(), 1)).not.toThrow();
  });

  it('clearSpeech で消える', () => {
    const s = makeSpeechState();
    pushSpeech(s, [req('p1', 'roran', 'あ')]);
    clearSpeech(s);
    expect(s.current).toBeNull();
  });
});
```

`src/ui/layout.test.ts` の `describe('bubbleRectAt')` と `describe('bubbleLines')`（あれば）をまるごと削除し、次を足す。import から `BUBBLE_FONT_PX` / `BUBBLE_LINE_H` / `BUBBLE_PAD` / `bubbleLines` / `bubbleRectAt` を外し、`speechLines` を足す。

```ts
describe('speechLines', () => {
  it('明示的な改行で分かれる', () => {
    expect(speechLines('あ\nい')).toEqual(['あ', 'い']);
  });

  it('折り返し幅を超えたら折り返す', () => {
    expect(speechLines('あ'.repeat(60)).length).toBeGreaterThan(1);
  });

  it('半角文字は全角の半分の幅で数える', () => {
    // 同じ文字数なら、半角だけの行は全角だけの行より折り返しが少ない
    const half = speechLines('a'.repeat(40)).length;
    const full = speechLines('あ'.repeat(40)).length;
    expect(half).toBeLessThan(full);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/ui/speech.test.ts src/ui/layout.test.ts`
Expected: FAIL。`./speech` が無く、`speechLines` も export されていない

- [ ] **Step 3: `speech.ts` を実装する**

`src/ui/speech.ts` を新規作成する。

```ts
import type { DialogueRequest } from '../core/dialogue';

/** セリフが消えるまでの秒数。読ませるのではなく気づかせるための長さ */
export const SPEECH_DURATION = 3.0;

export type Speech = { uid: string; defId: string; text: string; ttl: number };

/**
 * 下パネルのセリフ欄は1件しか出せないので、状態も1件だけ持つ。
 * 新しい発話は前の発話を上書きする
 */
export type SpeechState = { current: Speech | null };

export function makeSpeechState(): SpeechState {
  return { current: null };
}

/**
 * 同じ tick に複数の発話が来たときは先頭だけを採る。pickDialogue は優先順位
 * （rival → first → skill → levelup → pinch → win → retire）の昇順で返すので、
 * 末尾で上書きすると優先度の低いセリフが勝ってしまう
 */
export function pushSpeech(state: SpeechState, reqs: DialogueRequest[]): void {
  const first = reqs[0];
  if (first === undefined) return;
  state.current = {
    uid: first.uid, defId: first.speaker.id, text: first.text, ttl: SPEECH_DURATION,
  };
}

export function tickSpeech(state: SpeechState, dt: number): void {
  const s = state.current;
  if (s === null) return;
  s.ttl -= dt;
  if (s.ttl <= 0) state.current = null;
}

export function clearSpeech(state: SpeechState): void {
  state.current = null;
}
```

- [ ] **Step 4: `layout.ts` の吹き出しをセリフ欄に置き換える**

`src/ui/layout.ts` の `BUBBLE_FONT_PX` から `bubbleRectAt` までをすべて削除し、次を置く。`import { wrapText } from './talk';` は残す。`LOGICAL_W` と `Vec2` の import が未使用になれば外す。

```ts
export const SPEECH_FONT_PX = 18;
export const SPEECH_LINE_H = 22;
/** セリフ欄に出す最大行数。これを超える行は切る */
export const SPEECH_MAX_LINES = 2;
/** 本文の描き始め（顔のぶん右へ寄せる） */
export const SPEECH_BODY_X = 68;
/** 本文の折り返し幅。MESSAGE_BAR から顔と右の余白を引いた残り */
const SPEECH_CONTENT_W = MESSAGE_BAR.w - SPEECH_BODY_X - 12;

/**
 * 幅の見積り。measureText は使わない（レイアウト側が描画コンテキストを持たないため）。
 * 半角は全角の半分で数える。「Lv3」のような半角混在を全角前提で数えると幅を
 * 過大に見積もり、入るはずの行が折り返される
 */
const measure = (t: string): number => {
  let w = 0;
  for (const ch of t) w += /^[\x20-\x7e]$/.test(ch) ? SPEECH_FONT_PX / 2 : SPEECH_FONT_PX;
  return w;
};

/**
 * セリフ欄に実際に描く行。描画はこの1本を通す。
 * 別々に折り返すと箱から文字がはみ出す
 */
export function speechLines(text: string): string[] {
  return wrapText(text, measure, SPEECH_CONTENT_W);
}
```

- [ ] **Step 5: セリフ欄を描く**

`src/ui/screens.ts` の `drawBubble` を次に差し替える。import の `BUBBLE_*` / `bubbleLines` / `bubbleRectAt` を `SPEECH_BODY_X` / `SPEECH_FONT_PX` / `SPEECH_LINE_H` / `SPEECH_MAX_LINES` / `speechLines` に替え、`Bubble` の型 import を `Speech` に替える。

```ts
/** 戦闘中のセリフ。下パネルの上段に出し、時間は止めない */
export function drawSpeechBar(
  ctx: CanvasRenderingContext2D,
  reg: Registry,
  speech: Speech,
  images: ImageCache,
): void {
  const r = MESSAGE_BAR;
  panel(ctx, r, '#f7f3e6');

  const def = lookupDef(reg, speech.defId) ?? FALLBACK_DEF;
  drawFace(ctx, { x: r.x + 34, y: r.y + r.h / 2 }, 24, def, images);

  ctx.fillStyle = '#1a1a1a';
  ctx.font = `${SPEECH_FONT_PX}px sans-serif`;
  const lines = speechLines(speech.text).slice(0, SPEECH_MAX_LINES);
  // 行数によらず縦中央に来るように、上端からの余白を行数から出す
  const top = r.y + (r.h - lines.length * SPEECH_LINE_H) / 2 + SPEECH_FONT_PX * 0.875;
  lines.forEach((line, i) => {
    ctx.fillText(line, r.x + SPEECH_BODY_X, top + i * SPEECH_LINE_H);
  });
}
```

- [ ] **Step 6: 吹き出しの当たり判定を消す**

`src/ui/input.ts` の `PointerStart` から `bubbleUid` を、`MapGesture` から `dismissBubble` を削除し、`resolveMapGesture` の先頭の分岐を消す。

```ts
export type PointerStart = {
  /** ポインターを下ろした位置にいた味方。地面なら null */
  uid: string | null;
  startMap: Vec2;
  /** 下ろした時点で、その味方がすでに選択されていたか */
  wasSelected: boolean;
  /** このジェスチャを開始した指/ポインターの識別子。マルチタッチ時に他の指のイベントと区別するために使う */
  pointerId: number;
};

export type MapGesture =
  | { type: 'none' }
  | { type: 'select'; uid: string }
  | { type: 'deselect' }
  | { type: 'moveUnit'; uid: string; dest: Vec2 };

/**
 * マップ上のポインター操作を、フェーズに依存しないジェスチャへ変換する。
 * moveUnit をコマンドにするか再配置にするかは呼び出し側が決める。
 */
export function resolveMapGesture(
  start: PointerStart,
  endMap: Vec2,
  selected: string | null,
): MapGesture {
  const moved = distance(start.startMap, endMap) > TAP_SLOP;

  if (start.uid !== null) {
    if (moved) return { type: 'moveUnit', uid: start.uid, dest: endMap };
    return start.wasSelected ? { type: 'deselect' } : { type: 'select', uid: start.uid };
  }

  if (moved || selected === null) return { type: 'none' };
  return { type: 'moveUnit', uid: selected, dest: endMap };
}
```

`src/ui/input.test.ts` の `start()` ヘルパから `bubbleUid: null` を外し、`dismissBubble` を検証しているテストを削除する。

- [ ] **Step 7: `main.ts` の配線を差し替える**

次の変更を入れる。

1. import を差し替える
   - `import { clearBubbles, dismissBubble, makeBubbleState, pushBubbles, tickBubbles } from './ui/bubbles';` → `import { clearSpeech, makeSpeechState, pushSpeech, tickSpeech } from './ui/speech';`
   - `drawBubble` → `drawSpeechBar`（`./ui/screens` から）
   - `bubbleRectAt` を `./ui/layout` の import から外す
   - `mapToLogical` が他で使われていなければ import から外す
2. `const bubbles = makeBubbleState();` → `const speech = makeSpeechState();`
3. `bubbleAt` 関数をまるごと削除する
4. `beginMapPointer` の `pointerStart = { ... }` から `bubbleUid` の行を削除する
5. `onPointerUp` の `case 'dismissBubble':` を削除する
6. `beginStage` の `clearBubbles(bubbles);` → `clearSpeech(speech);`
7. `update` の `tickBubbles(bubbles, dt);` → `tickSpeech(speech, dt);`
8. `update` の `pushBubbles(bubbles, pickDialogue(...));` → `pushSpeech(speech, pickDialogue(battle.reg, battle.events));`
9. 勝利時の `clearBubbles(bubbles);` → `clearSpeech(speech);`
10. `render()` の `case 'battle':` の吹き出しループを差し替える

```ts
    case 'battle':
      if (battle) {
        drawBattle(ctx, registry, battle, selected, effects, escorts, images, anim);
        drawBottomBar(ctx, registry, battle, selected, escorts, images);
        if (speech.current !== null) drawSpeechBar(ctx, registry, speech.current, images);
      }
      break;
```

- [ ] **Step 8: 吹き出しのモジュールを削除する**

```bash
git rm src/ui/bubbles.ts src/ui/bubbles.test.ts
```

- [ ] **Step 9: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功

- [ ] **Step 10: コミット**

```bash
git add -A src/ui src/main.ts
git commit -m "feat: 戦闘中のセリフを頭上の吹き出しから下パネル上段へ移す"
```

---

### Task 6: 敵の速度を下げ、索敵範囲を隠す（項目8・10）

**Files:**
- Modify: `assets/enemies/narazumono.json`、`assets/enemies/tatemochi.json`、`assets/enemies/garum.json`
- Modify: `src/render/objectives-view.ts`（`sightCircles` → `alertMarks`）
- Modify: `src/render/objectives-view.test.ts`
- Modify: `src/render/draw.ts`（`drawSightRanges` → `drawAlertMarks`）

**Interfaces:**
- Consumes: `Unit`（既存、`src/core/types.ts`）、`drawHalf`（既存、`src/render/sprites.ts`）
- Produces: `type AlertMark = { pos: Vec2; defId: string }`、`alertMarks(units: Unit[]): AlertMark[]`

- [ ] **Step 1: 失敗するテストを書く**

`src/render/objectives-view.test.ts` の `describe('sightCircles')` をまるごと削除し、次に差し替える。import の `sightCircles` を `alertMarks` に替える。`enemy()` ヘルパは既存のものを使う。

```ts
describe('alertMarks', () => {
  it('追跡中の敵を返す', () => {
    const u = enemy('e1', 10, 20, { def: { kind: 'sentry', sightRange: 100 }, mode: 'chase', targetUid: 'p1', home: { x: 10, y: 20 } });
    expect(alertMarks([u])).toEqual([{ pos: { x: 10, y: 20 }, defId: u.defId }]);
  });

  it('追跡していない敵は返さない', () => {
    const u = enemy('e1', 10, 20, { def: { kind: 'sentry', sightRange: 100 }, mode: 'idle', targetUid: null, home: { x: 10, y: 20 } });
    expect(alertMarks([u])).toEqual([]);
  });

  it('aggressive は返さない（常に追ってくるので「気づかれた」印にならない）', () => {
    const u = enemy('e1', 10, 20, { def: { kind: 'aggressive' }, mode: 'chase', targetUid: 'p1', home: { x: 10, y: 20 } });
    expect(alertMarks([u])).toEqual([]);
  });

  it('退場した敵は返さない', () => {
    const u = enemy('e1', 10, 20, { def: { kind: 'guard', post: { x: 0, y: 0 }, leash: 10, sightRange: 100 }, mode: 'chase', targetUid: 'p1', home: { x: 10, y: 20 } });
    expect(alertMarks([{ ...u, retired: true }])).toEqual([]);
  });

  it('ai を持たない味方は返さない', () => {
    const ally = { uid: 'p1', defId: 'roran', pos: { x: 1, y: 2 }, side: 'player', retired: false, ai: null } as unknown as Unit;
    expect(alertMarks([ally])).toEqual([]);
  });
});
```

`enemy()` ヘルパは `defId` を持たないキャストなので、`defId` を足した版に差し替える。

```ts
function enemy(uid: string, x: number, y: number, ai: Unit['ai']): Unit {
  return { uid, defId: 'narazumono', pos: { x, y }, side: 'enemy', retired: false, ai } as unknown as Unit;
}
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/render/objectives-view.test.ts`
Expected: FAIL。`alertMarks` が export されていない

- [ ] **Step 3: `alertMarks` を実装する**

`src/render/objectives-view.ts` の `SightCircle` と `sightCircles` を次に差し替える。

```ts
export type AlertMark = { pos: Vec2; defId: string };

/**
 * 気づかれた敵。索敵範囲そのものは見せない。
 * 範囲が見えると「どこまでなら近づけるか」を測る作業になって緊張感が削がれるため、
 * 気づかれたことだけを伝える。aggressive は開始時から追ってくるので対象外
 * （常に印が出てしまい、「気づかれた」という意味を持たなくなる）
 */
export function alertMarks(units: Unit[]): AlertMark[] {
  const out: AlertMark[] = [];
  for (const u of units) {
    if (u.retired || u.side !== 'enemy' || u.ai === null) continue;
    if (u.ai.def.kind === 'aggressive' || u.ai.mode !== 'chase') continue;
    out.push({ pos: { ...u.pos }, defId: u.defId });
  }
  return out;
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npx vitest run src/render/objectives-view.test.ts`
Expected: PASS

- [ ] **Step 5: 描画を差し替える**

`src/render/draw.ts` の `COLORS` から `sight` と `sightAlert` を削除し、`alert: '#ff5a5a'` を足す。import の `sightCircles` を `alertMarks` に替える。

`drawSightRanges` を次に差し替える。

```ts
/** 気づかれた敵の頭上に「！」を出す。索敵範囲そのものは見せない */
function drawAlertMarks(ctx: CanvasRenderingContext2D, reg: Registry, state: BattleState): void {
  ctx.fillStyle = COLORS.alert;
  ctx.font = 'bold 22px sans-serif';
  ctx.textAlign = 'center';
  for (const m of alertMarks(state.units)) {
    const p = mapToLogical(m.pos);
    const half = drawHalf(defOf(reg, m.defId), UNIT_R);
    ctx.fillText('！', p.x, p.y - half - 10);
  }
  ctx.textAlign = 'left';
}
```

`drawBattle` の呼び出しを差し替える。ユニットより上に出すので、`drawUnits` の後へ移す。

```ts
  drawTerrain(ctx, state);
  drawVictoryMarker(ctx, state.stage);
  drawGoalMarkers(ctx, reg, state, selected);
  drawBonds(ctx, state);
  drawUnits(ctx, reg, state, selected, effects, images, anim);
  drawAlertMarks(ctx, reg, state);
  drawProjectiles(ctx, state);
  drawEscortMarks(ctx, state, escorts);
  drawEffects(ctx, effects);
  drawTopBar(ctx, state);
```

- [ ] **Step 6: 敵の速度を25%下げる**

| ファイル | `speed` |
|---|---|
| `assets/enemies/narazumono.json` | `45` → `34` |
| `assets/enemies/tatemochi.json` | `35` → `26` |
| `assets/enemies/garum.json` | `55` → `41` |

- [ ] **Step 7: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功

- [ ] **Step 8: コミット**

```bash
git add assets/enemies src/render/objectives-view.ts src/render/objectives-view.test.ts src/render/draw.ts
git commit -m "feat: 敵の速度を下げ、索敵範囲の円をやめて気づかれた印だけ出す"
```

---

### Task 7: 時間湧きの敵を足す（項目9）

**Files:**
- Create: `src/core/spawns.ts`、`src/core/spawns.test.ts`
- Modify: `src/engine/schema.ts`（`SpawnerDef`、`StageDef.spawners`、`readSpawners`）
- Modify: `src/engine/schema.test.ts`
- Modify: `src/engine/registry.ts`（湧き口の defId の相互参照チェック）
- Modify: `src/core/types.ts`（`SimEvent` に `enemySpawned`、`BattleState` に `spawnCounts`）
- Modify: `src/core/state.ts`（`makeEnemyUnit` の切り出し、`spawnCounts` の初期化）
- Modify: `src/core/sim.ts`（`runSpawns`）
- Modify: `src/core/sim.test.ts`
- Modify: `src/render/effects.ts`（`spawn` エフェクト）
- Modify: `src/render/draw.ts`（`spawn` の描画）
- Modify: `assets/stages/stage2.json`、`assets/stages/stage3.json`

**Interfaces:**
- Consumes: `AiDef`（既存、`src/engine/schema.ts`）、`makeUnit`（`src/core/state.ts` 内の private 関数）
- Produces: `type SpawnerDef = { defId: string; pos: Vec2; firstAfter: number; every: number; total: number }`、`StageDef.spawners: SpawnerDef[]`、`dueSpawns(spawners, counts, time): { index: number; spawner: SpawnerDef }[]`、`makeEnemyUnit(reg, uid, defId, pos, ai): Unit`、`BattleState.spawnCounts: number[]`、`SimEvent` の `{ type: 'enemySpawned'; uid: string; defId: string; pos: Vec2 }`

- [ ] **Step 1: 失敗するテストを書く**

`src/core/spawns.test.ts` を新規作成する。

```ts
import { describe, expect, it } from 'vitest';
import { dueSpawns } from './spawns';
import type { SpawnerDef } from '../engine/schema';

const sp = (over: Partial<SpawnerDef> = {}): SpawnerDef => ({
  defId: 'narazumono', pos: { x: 0, y: 0 }, firstAfter: 10, every: 5, total: 3, ...over,
});

describe('dueSpawns', () => {
  it('firstAfter より前は湧かない', () => {
    expect(dueSpawns([sp()], [0], 9.9)).toEqual([]);
  });

  it('firstAfter に達したら1体目が湧く', () => {
    expect(dueSpawns([sp()], [0], 10)).toEqual([{ index: 0, spawner: sp() }]);
  });

  it('2体目は every ぶん待つ', () => {
    expect(dueSpawns([sp()], [1], 14.9)).toEqual([]);
    expect(dueSpawns([sp()], [1], 15)).toHaveLength(1);
  });

  it('total に達したら湧かない', () => {
    expect(dueSpawns([sp()], [3], 9999)).toEqual([]);
  });

  it('1回の呼び出しで湧き口ごとに最大1体', () => {
    expect(dueSpawns([sp()], [0], 9999)).toHaveLength(1);
  });

  it('湧き口ごとに独立して数える', () => {
    const r = dueSpawns([sp({ total: 1 }), sp({ firstAfter: 0 })], [1, 0], 10);
    expect(r.map((d) => d.index)).toEqual([1]);
  });

  it('湧き口が無ければ何も返さない', () => {
    expect(dueSpawns([], [], 9999)).toEqual([]);
  });
});
```

`src/core/sim.test.ts` に追記する（`testRegistry` と `createBattleState` を使う既存のヘルパに合わせること）。

```ts
describe('時間湧き', () => {
  function spawnStage() {
    const reg = testRegistry();
    const stage = reg.stages.find((s) => s.spawners.length > 0);
    if (!stage) throw new Error('spawners を持つステージが assets に無い');
    const progress: Record<string, CharProgress> = {};
    for (const id of reg.units.keys()) progress[id] = { level: 1, xp: 0 };
    const state = createBattleState(reg, stage, progress, 1);
    beginBattle(state);
    return { stage, state };
  }

  it('firstAfter に達すると敵が1体増える', () => {
    const { stage, state } = spawnStage();
    const before = state.units.filter((u) => u.side === 'enemy').length;
    state.time = stage.spawners[0]!.firstAfter;
    step(state, [], 1 / 60);
    expect(state.units.filter((u) => u.side === 'enemy').length).toBe(before + 1);
    expect(state.spawnCounts[0]).toBe(1);
  });

  it('湧いた敵は aggressive で追ってくる', () => {
    const { stage, state } = spawnStage();
    state.time = stage.spawners[0]!.firstAfter;
    step(state, [], 1 / 60);
    const spawned = state.units[state.units.length - 1]!;
    expect(spawned.side).toBe('enemy');
    expect(spawned.ai!.def.kind).toBe('aggressive');
    expect(spawned.pos).toEqual(stage.spawners[0]!.pos);
  });

  it('enemySpawned イベントを出す', () => {
    const { stage, state } = spawnStage();
    state.time = stage.spawners[0]!.firstAfter;
    step(state, [], 1 / 60);
    expect(state.events.some((e) => e.type === 'enemySpawned')).toBe(true);
  });

  it('total を超えて湧かない', () => {
    const { stage, state } = spawnStage();
    const before = state.units.filter((u) => u.side === 'enemy').length;
    const total = stage.spawners.reduce((n, s) => n + s.total, 0);
    // 湧き口ごとに1 tick 1体なので、total 回ぶん十分に時間を進めて回す
    for (let i = 0; i < total + 5; i++) {
      state.time += 1000;
      step(state, [], 1 / 60);
    }
    expect(state.units.filter((u) => u.side === 'enemy').length).toBe(before + total);
  });
});
```

`src/engine/schema.test.ts` に追記する（`stageRaw` は Task 2 で足したヘルパ）。

```ts
describe('validateStageDef: spawners', () => {
  it('書かなければ空配列', () => {
    const r = validateStageDef('assets/stages/t.json', stageRaw());
    expect(r.ok).toBe(true);
    if (r.ok) expect(r.value.spawners).toEqual([]);
  });

  it('読める', () => {
    const raw = stageRaw({
      spawners: [{ defId: 'narazumono', pos: { x: 48, y: 48 }, firstAfter: 5, every: 10, total: 2 }],
    });
    const r = validateStageDef('assets/stages/t.json', raw);
    expect(r.ok).toBe(true);
    if (r.ok) expect(r.value.spawners[0]!.total).toBe(2);
  });

  it('歩けないマスはエラー', () => {
    const raw = stageRaw({
      spawners: [{ defId: 'narazumono', pos: { x: 0, y: 0 }, firstAfter: 5, every: 10, total: 2 }],
    });
    expect(validateStageDef('assets/stages/t.json', raw).ok).toBe(false);
  });

  it('total が 0 以下はエラー', () => {
    const raw = stageRaw({
      spawners: [{ defId: 'narazumono', pos: { x: 48, y: 48 }, firstAfter: 5, every: 10, total: 0 }],
    });
    expect(validateStageDef('assets/stages/t.json', raw).ok).toBe(false);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/core/spawns.test.ts src/core/sim.test.ts src/engine/schema.test.ts`
Expected: FAIL。`./spawns` が無く、`stage.spawners` も存在しない

- [ ] **Step 3: スキーマに湧き口を足す**

`src/engine/schema.ts` の `EnemyPlacement` の下に足す。

```ts
/** 時間で敵を湧かせる口。湧いた敵は aggressive 固定でプレイヤーを追う */
export type SpawnerDef = {
  defId: string;
  pos: Vec2;
  /** 戦闘開始から1体目までの秒数 */
  firstAfter: number;
  /** 2体目以降の間隔（秒） */
  every: number;
  /** この湧き口から出る総数。上限を必須にしないと持久戦で詰む */
  total: number;
};
```

`StageDef` に `spawners: SpawnerDef[];` を `enemies` の次に足す。

`readPlacement` の下に読み取り関数を足す。

```ts
function readSpawners(
  ctx: Ctx, v: unknown, checkWalkable: (path: string, pos: Vec2) => void,
): SpawnerDef[] {
  if (v === undefined) return [];
  const arr = requireArray(ctx, 'spawners', v) ?? [];
  const out: SpawnerDef[] = [];
  arr.forEach((item, i) => {
    const path = `spawners[${i}]`;
    const o = requireObject(ctx, path, item);
    if (!o) return;
    const pos = requireVec2(ctx, `${path}.pos`, o.pos) ?? { x: 0, y: 0 };
    checkWalkable(`${path}.pos`, pos);
    out.push({
      defId: requireString(ctx, `${path}.defId`, o.defId) ?? '',
      pos,
      firstAfter: requireNumber(ctx, `${path}.firstAfter`, o.firstAfter, { min: 0 }) ?? 0,
      every: requireNumber(ctx, `${path}.every`, o.every, { min: 1 }) ?? 1,
      total: requireNumber(ctx, `${path}.total`, o.total, { min: 1, int: true }) ?? 1,
    });
  });
  return out;
}
```

`validateStageDef` の `stage` 組み立てに `spawners: readSpawners(ctx, o.spawners, checkWalkable),` を `enemies` の次に足す。

- [ ] **Step 4: レジストリで defId を照合する**

`src/engine/registry.ts` の `stage.enemies.forEach(...)` の直後に足す。

```ts
    stage.spawners.forEach((s, i) => {
      if (!reg.enemies.has(s.defId)) {
        errors.push({ file, path: `spawners[${i}].defId`, reason: `enemies に ない id: ${s.defId}` });
      }
    });
```

- [ ] **Step 5: 型を足す**

`src/core/types.ts` の `SimEvent` の union に足す。

```ts
  | { type: 'enemySpawned'; uid: string; defId: string; pos: Vec2 }
```

`BattleState` の `nextEnemyUid: number;` の次に足す。

```ts
  /** 湧き口ごとに、これまで湧かせた数。stage.spawners と同じ長さ */
  spawnCounts: number[];
```

- [ ] **Step 6: `dueSpawns` を実装する**

`src/core/spawns.ts` を新規作成する。

```ts
import type { SpawnerDef } from '../engine/schema';

export type DueSpawn = { index: number; spawner: SpawnerDef };

/**
 * いま湧かせるべき敵を返す。1回の呼び出しで湧き口ごとに最大1体。
 * n 体目が出る時刻は firstAfter + every × (n - 1)。
 * counts[index] が total に達した湧き口は返さない
 */
export function dueSpawns(
  spawners: SpawnerDef[], counts: number[], time: number,
): DueSpawn[] {
  const out: DueSpawn[] = [];
  spawners.forEach((spawner, index) => {
    const done = counts[index] ?? 0;
    if (done >= spawner.total) return;
    if (time < spawner.firstAfter + spawner.every * done) return;
    out.push({ index, spawner });
  });
  return out;
}
```

- [ ] **Step 7: 敵ユニットの生成を切り出す**

`src/core/state.ts` の `createBattleState` の敵生成を、再利用できる関数に切り出す。

```ts
/** 敵ユニットを1体つくる。ステージ開始時の配置と時間湧きの両方がここを通る */
export function makeEnemyUnit(
  reg: Registry, uid: string, defId: string, pos: Vec2, ai: AiDef,
): Unit {
  const def = reg.enemies.get(defId);
  if (!def) throw new Error(`はいちに しらない てき: ${defId}`);
  return makeUnit({ uid, def, side: 'enemy', controller: 'ai', pos, level: 1, xp: 0, ai });
}
```

`createBattleState` の敵生成と戻り値を差し替える。

```ts
  let nextEnemyUid = 1;
  const enemies = stage.enemies.map((placement) =>
    makeEnemyUnit(reg, `e${nextEnemyUid++}`, placement.defId, placement.pos, placement.ai));

  return {
    reg,
    stage,
    grid,
    fields: makeFieldCache(),
    time: 0,
    phase: 'placement',
    units: [...roster, ...enemies],
    events: [],
    counters: {},
    rng: makeRng(seed),
    nextEnemyUid,
    spawnCounts: stage.spawners.map(() => 0),
    projectiles: [],
    nextProjectileId: 1,
  };
```

- [ ] **Step 8: シムに湧きをつなぐ**

`src/core/sim.ts` に足す。

```ts
/** 時間湧き。湧いた敵は aggressive 固定でプレイヤーを追う */
function runSpawns(state: BattleState): void {
  for (const due of dueSpawns(state.stage.spawners, state.spawnCounts, state.time)) {
    state.spawnCounts[due.index] = (state.spawnCounts[due.index] ?? 0) + 1;
    const unit = makeEnemyUnit(
      state.reg, `e${state.nextEnemyUid++}`, due.spawner.defId, due.spawner.pos,
      { kind: 'aggressive' },
    );
    state.units.push(unit);
    state.events.push({
      type: 'enemySpawned', uid: unit.uid, defId: unit.defId, pos: { ...unit.pos },
    });
  }
}
```

`step()` の `state.time += dt;` の直後に `runSpawns(state);` を足す。import に `import { dueSpawns } from './spawns';` と `import { makeEnemyUnit } from './state';` を足す。

- [ ] **Step 9: 出現エフェクトを足す**

`src/render/effects.ts` に足す。

```ts
export const SPAWN_DURATION = 0.5;
```

`Effect` の union に `| { kind: 'spawn'; pos: Vec2; ttl: number }` を足し、`spawnEffects` の分岐に足す。

```ts
    } else if (ev.type === 'enemySpawned') {
      state.items.push({ kind: 'spawn', pos: { ...ev.pos }, ttl: SPAWN_DURATION });
    }
```

`src/render/draw.ts` の `drawEffects` の switch に足す（import に `SPAWN_DURATION` を追加）。

```ts
      case 'spawn': {
        const p = mapToLogical(e.pos);
        const ratio = Math.max(0, e.ttl / SPAWN_DURATION);
        // 外から内へ縮む輪。「出てきた」と読めるように
        ctx.strokeStyle = `rgba(255, 90, 90, ${1 - ratio})`;
        ctx.lineWidth = 3;
        ctx.beginPath();
        ctx.arc(p.x, p.y, EFFECT_R + ratio * 24, 0, Math.PI * 2);
        ctx.stroke();
        break;
      }
```

- [ ] **Step 10: ステージに湧き口を置く**

`assets/stages/stage2.json` の `enemies` 配列の直後に足す。

```json
  "spawners": [
    { "defId": "narazumono", "pos": { "x": 400, "y": 80 }, "firstAfter": 20, "every": 15, "total": 3 }
  ],
```

`assets/stages/stage3.json` の `enemies` 配列の直後に足す。

```json
  "spawners": [
    { "defId": "narazumono", "pos": { "x": 80, "y": 80 }, "firstAfter": 15, "every": 12, "total": 4 }
  ],
```

`assets/stages/stage1.json` には置かない（導入ステージ）。

- [ ] **Step 11: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功

- [ ] **Step 12: コミット**

```bash
git add src/core/spawns.ts src/core/spawns.test.ts src/core/sim.ts src/core/sim.test.ts src/core/state.ts src/core/types.ts src/engine src/render/effects.ts src/render/draw.ts assets/stages
git commit -m "feat: 時間湧きの敵をステージ定義から出せるようにする"
```

---

### Task 8: 成長の調整値を `assets/growth.json` に出す（項目11の土台）

**Files:**
- Create: `assets/growth.json`
- Modify: `src/engine/schema.ts`（`GrowthDef`、`validateGrowthFile`）
- Modify: `src/engine/schema.test.ts`
- Modify: `src/engine/registry.ts`（`Registry.growth`、読み込み、欠落時のエラー）
- Modify: `src/engine/registry.test.ts`

**Interfaces:**
- Consumes: `requireObject` / `requireNumber` / `makeCtx`（既存、`src/engine/schema.ts`）
- Produces: `type GrowthDef = { maxLevel: number; xpPerLevel: number; hpPerLevel: number; levelsPerPower: number; hitXp: number; healXp: number; assistRatio: number; clearXp: number }`、`validateGrowthFile(file, raw): Validated<GrowthDef>`、`Registry.growth: GrowthDef`

- [ ] **Step 1: 失敗するテストを書く**

`src/engine/schema.test.ts` に追記する。

```ts
describe('validateGrowthFile', () => {
  const raw = () => ({
    maxLevel: 12, xpPerLevel: 12, hpPerLevel: 1, levelsPerPower: 3,
    hitXp: 1, healXp: 1, assistRatio: 0.5, clearXp: 10,
  });

  it('正しい形を読む', () => {
    const r = validateGrowthFile('assets/growth.json', raw());
    expect(r.ok).toBe(true);
    if (r.ok) expect(r.value).toEqual(raw());
  });

  it('maxLevel が 0 以下はエラー', () => {
    expect(validateGrowthFile('assets/growth.json', { ...raw(), maxLevel: 0 }).ok).toBe(false);
  });

  it('hitXp が小数はエラー（経験値が小数になるとリザルトに出てしまう）', () => {
    expect(validateGrowthFile('assets/growth.json', { ...raw(), hitXp: 0.5 }).ok).toBe(false);
  });

  it('assistRatio が 1 を超えるとエラー', () => {
    expect(validateGrowthFile('assets/growth.json', { ...raw(), assistRatio: 1.5 }).ok).toBe(false);
  });

  it('フィールドが欠けているとエラー', () => {
    const o: Record<string, unknown> = raw();
    delete o.clearXp;
    expect(validateGrowthFile('assets/growth.json', o).ok).toBe(false);
  });
});
```

`src/engine/registry.test.ts` に追記する。

```ts
describe('growth', () => {
  it('assets/growth.json を読んで registry に入れる', () => {
    const reg = testRegistry();
    expect(reg.growth.maxLevel).toBe(12);
    expect(reg.growth.xpPerLevel).toBe(12);
    expect(reg.growth.assistRatio).toBe(0.5);
  });
});
```

`testRegistry` の import が無ければ `import { testRegistry } from '../core/testing';` を足す。

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/engine/schema.test.ts src/engine/registry.test.ts`
Expected: FAIL。`validateGrowthFile` が無く、`reg.growth` も無い

- [ ] **Step 3: `assets/growth.json` を作る**

```json
{
  "maxLevel": 12,
  "xpPerLevel": 12,
  "hpPerLevel": 1,
  "levelsPerPower": 3,
  "hitXp": 1,
  "healXp": 1,
  "assistRatio": 0.5,
  "clearXp": 10
}
```

- [ ] **Step 4: 検証を書く**

`src/engine/schema.ts` の `validateLinesFile` の下に足す。

```ts
/**
 * 成長の調整値。レベルアップの頻度（xpPerLevel）と1レベルの強化量（hpPerLevel /
 * levelsPerPower）を別々に持つので、片方だけを動かして調整できる。
 * hitXp / healXp / clearXp を整数に縛るのは、経験値が小数になるとリザルト画面に
 * 4.5/12 のような値が出るため
 */
export type GrowthDef = {
  maxLevel: number;
  /** レベル n から n+1 に必要な経験値は n × xpPerLevel */
  xpPerLevel: number;
  /** 1レベルあたりの最大HPの増加 */
  hpPerLevel: number;
  /** 攻撃力が1上がるのに必要なレベル数 */
  levelsPerPower: number;
  /** 命中1回ごとに攻撃側へ入る経験値 */
  hitXp: number;
  /** 回復1回ごとに回復側へ入る経験値 */
  healXp: number;
  /** 撃破時、とどめ以外でダメージを与えた味方へ配る割合 */
  assistRatio: number;
  /** ステージクリア時、退場していない味方全員へ入る経験値 */
  clearXp: number;
};

export function validateGrowthFile(file: string, raw: unknown): Validated<GrowthDef> {
  const ctx = makeCtx(file);
  const o = requireObject(ctx, '', raw);
  if (!o) return { ok: false, errors: ctx.errors };
  const growth: GrowthDef = {
    maxLevel: requireNumber(ctx, 'maxLevel', o.maxLevel, { min: 1, int: true }) ?? 1,
    xpPerLevel: requireNumber(ctx, 'xpPerLevel', o.xpPerLevel, { min: 1, int: true }) ?? 1,
    hpPerLevel: requireNumber(ctx, 'hpPerLevel', o.hpPerLevel, { min: 0, int: true }) ?? 0,
    levelsPerPower: requireNumber(ctx, 'levelsPerPower', o.levelsPerPower, { min: 1, int: true }) ?? 1,
    hitXp: requireNumber(ctx, 'hitXp', o.hitXp, { min: 0, int: true }) ?? 0,
    healXp: requireNumber(ctx, 'healXp', o.healXp, { min: 0, int: true }) ?? 0,
    assistRatio: requireNumber(ctx, 'assistRatio', o.assistRatio, { min: 0, max: 1 }) ?? 0,
    clearXp: requireNumber(ctx, 'clearXp', o.clearXp, { min: 0, int: true }) ?? 0,
  };
  return finish(ctx, growth);
}
```

- [ ] **Step 5: レジストリに足す**

`src/engine/registry.ts` を次のように変える。

1. import に `validateGrowthFile` と `GrowthDef` を足す
2. `Registry` に `growth: GrowthDef;` を足す
3. 仮値と「読めたか」のフラグを置く

```ts
/**
 * growth.json が無い・壊れているときの仮値。errors が積まれた時点でゲームは
 * 起動しないので実際には使われない（schema.ts のヘルパと同じ流儀）
 */
const FALLBACK_GROWTH: GrowthDef = {
  maxLevel: 1, xpPerLevel: 1, hpPerLevel: 0, levelsPerPower: 1,
  hitXp: 0, healXp: 0, assistRatio: 0, clearXp: 0,
};
```

4. `reg` の初期値に `growth: FALLBACK_GROWTH,` を足す
5. ファイル分岐に足す（`baseName(path) === 'titles'` の分岐の次、`else` の手前）

```ts
    } else if (baseName(path) === 'growth') {
      sawGrowth = true;
      take(validateGrowthFile(path, raw), (d) => { reg.growth = d; });
```

6. 関数の先頭に `let sawGrowth = false;` を置き、ファイルのループの直後に足す

```ts
  if (!sawGrowth) {
    errors.push({ file: 'assets/growth.json', path: '', reason: 'growth.json が ない' });
  }
```

- [ ] **Step 6: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功

- [ ] **Step 7: コミット**

```bash
git add assets/growth.json src/engine
git commit -m "feat: 成長の調整値をassets/growth.jsonに出す"
```

---

### Task 9: ステータスの伸びを緩やかにし、上限を上げる（項目11）

**Files:**
- Modify: `src/core/progress.ts`（`MAX_LEVEL` / `XP_PER_LEVEL` 廃止、引数化）
- Modify: `src/core/progress.test.ts`
- Modify: `src/core/state.ts`（`statsForLevel`）
- Modify: `src/core/state.test.ts`
- Modify: `src/core/growth.ts`、`src/core/growth.test.ts`
- Modify: `src/ui/screens.ts`（`drawResult` の `xpToNext`）

**Interfaces:**
- Consumes: `GrowthDef`、`Registry.growth`（Task 8）
- Produces: `xpToNext(level: number, xpPerLevel: number): number`、`applyXp(p: CharProgress, gained: number, growth: GrowthDef): CharProgress`、`statsForLevel(def: UnitDef | EnemyDef, level: number, growth: GrowthDef): { maxHp: number; power: number }`

- [ ] **Step 1: 失敗するテストを書く**

`src/core/progress.test.ts` の `MAX_LEVEL` / `xpToNext` / `applyXp` を使っているテストを次の形に直す。

```ts
import { testRegistry } from './testing';

const growth = () => testRegistry().growth;

describe('xpToNext', () => {
  it('レベル × xpPerLevel', () => {
    expect(xpToNext(1, 12)).toBe(12);
    expect(xpToNext(4, 12)).toBe(48);
  });
});

describe('applyXp', () => {
  it('必要量に届けばレベルが上がる', () => {
    expect(applyXp({ level: 1, xp: 0 }, 12, growth())).toEqual({ level: 2, xp: 0 });
  });

  it('余りは次のレベルに繰り越す', () => {
    expect(applyXp({ level: 1, xp: 0 }, 15, growth())).toEqual({ level: 2, xp: 3 });
  });

  it('一度に複数レベル上がる', () => {
    // Lv2 まで 12、Lv3 まで 24
    expect(applyXp({ level: 1, xp: 0 }, 36, growth()).level).toBe(3);
  });

  it('上限に達したら増えない', () => {
    const g = growth();
    expect(applyXp({ level: g.maxLevel, xp: 0 }, 999, g)).toEqual({ level: g.maxLevel, xp: 0 });
  });
});
```

`src/core/state.test.ts` の `describe('statsForLevel')` を次に差し替える。

```ts
describe('statsForLevel', () => {
  it('レベル1 は基礎値どおり', () => {
    const reg = testRegistry();
    expect(statsForLevel(reg.units.get('roran')!, 1, reg.growth)).toEqual({ maxHp: 30, power: 6 });
  });

  it('HP は1レベルごとに+1', () => {
    const reg = testRegistry();
    expect(statsForLevel(reg.units.get('roran')!, 4, reg.growth).maxHp).toBe(33);
  });

  it('攻撃力は3レベルごとに+1', () => {
    const reg = testRegistry();
    expect(statsForLevel(reg.units.get('roran')!, 3, reg.growth).power).toBe(6);
    expect(statsForLevel(reg.units.get('roran')!, 4, reg.growth).power).toBe(7);
  });

  it('上限レベルでも現状の上限とほぼ同じ強さ', () => {
    const reg = testRegistry();
    expect(statsForLevel(reg.units.get('roran')!, 12, reg.growth)).toEqual({ maxHp: 41, power: 9 });
  });
});
```

同じファイルの「レベルが反映される」テストの期待値を直す（Lv3 は `maxHp: 32`、`power: 6`）。

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/core/progress.test.ts src/core/state.test.ts`
Expected: FAIL。`xpToNext` が引数1つ、`statsForLevel` が引数2つのまま

- [ ] **Step 3: `progress.ts` を引数化する**

`src/core/progress.ts` の先頭2つの定数を削除し、2関数を差し替える。

```ts
import type { GrowthDef } from '../engine/schema';

export function xpToNext(level: number, xpPerLevel: number): number {
  return level * xpPerLevel;
}

export function applyXp(p: CharProgress, gained: number, growth: GrowthDef): CharProgress {
  let level = p.level;
  let xp = p.xp;
  if (level >= growth.maxLevel) return { level, xp };

  xp += gained;
  while (level < growth.maxLevel && xp >= xpToNext(level, growth.xpPerLevel)) {
    xp -= xpToNext(level, growth.xpPerLevel);
    level += 1;
  }
  if (level >= growth.maxLevel) xp = 0;
  return { level, xp };
}
```

- [ ] **Step 4: `statsForLevel` を差し替える**

`src/core/state.ts` の `HP_PER_LEVEL` / `POWER_PER_LEVEL` を削除し、`statsForLevel` を差し替える。

```ts
/**
 * レベルからステータスを出す。HP は毎レベル、攻撃力は levelsPerPower レベルごとに上がる。
 * 攻撃力を小数にしないのは computeDamage が整数前提で組まれているため
 */
export function statsForLevel(
  def: UnitDef | EnemyDef, level: number, growth: GrowthDef,
): { maxHp: number; power: number } {
  const steps = Math.max(0, level - 1);
  return {
    maxHp: def.maxHp + steps * growth.hpPerLevel,
    power: def.power + Math.floor(steps / growth.levelsPerPower),
  };
}
```

`MakeUnitArgs` に `growth: GrowthDef;` を足し、`makeUnit` の中を `statsForLevel(a.def, a.level, a.growth)` にする。`createBattleState` の roster 生成と `makeEnemyUnit` の呼び出しに `growth: reg.growth,` を渡す。`import type` に `GrowthDef` を足す。

- [ ] **Step 5: 呼び出し側を直す**

`src/core/growth.ts` の `awardXp` を直す。

```ts
export function awardXp(state: BattleState, unit: Unit, amount: number): void {
  if (unit.retired || amount <= 0) return;

  const before = unit.level;
  const after = applyXp({ level: unit.level, xp: unit.xp }, amount, state.reg.growth);
  unit.level = after.level;
  unit.xp = after.xp;
  if (after.level === before) return;

  const def = state.reg.units.get(unit.defId) ?? state.reg.enemies.get(unit.defId);
  if (!def) return;
  const stats = statsForLevel(def, after.level, state.reg.growth);
  ...
```

`src/ui/screens.ts` の `drawResult` の `xpToNext(g.after.level)` を `xpToNext(g.after.level, reg.growth.xpPerLevel)` にする。

`src/core/growth.test.ts` の `MAX_LEVEL` を使っている箇所を `testRegistry().growth.maxLevel` に、`statsForLevel(def, 2)` を `statsForLevel(def, 2, reg.growth)` に直す。

- [ ] **Step 6: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功

- [ ] **Step 7: コミット**

```bash
git add src/core src/ui/screens.ts
git commit -m "feat: レベル上限を上げ、1レベルあたりの強化量を緩やかにする"
```

---

### Task 10: 経験値の入り口を増やす（項目11）

**Files:**
- Modify: `src/core/types.ts`（`Unit.damagedBy`）
- Modify: `src/core/state.ts`（`makeUnit` の初期値）
- Modify: `src/core/damage.ts`（`damagedBy` の記録）
- Modify: `src/core/growth.ts`（`awardXpForDefeats` → `awardXpForEvents`）
- Modify: `src/core/growth.test.ts`
- Modify: `src/core/sim.ts`（呼び出しの差し替え）
- Modify: `src/ui/flow.ts`（クリアボーナス）
- Modify: `src/ui/flow.test.ts`
- Modify: `assets/enemies/*.json`（`xpReward`）

**Interfaces:**
- Consumes: `awardXp(state, unit, amount)`（既存、`src/core/growth.ts`）、`Registry.growth`（Task 8）、`applyXp`（Task 9）
- Produces: `Unit.damagedBy: string[]`、`awardXpForEvents(state: BattleState): void`

- [ ] **Step 1: 失敗するテストを書く**

`src/core/growth.test.ts` に追記する。既存のヘルパ（`testRegistry` / `createBattleState` / `beginBattle`）に合わせること。

```ts
describe('awardXpForEvents', () => {
  function battle() {
    const reg = testRegistry();
    const stage = reg.stages[0]!;
    const progress: Record<string, CharProgress> = {};
    for (const id of reg.units.keys()) progress[id] = { level: 1, xp: 0 };
    const state = createBattleState(reg, stage, progress, 1);
    beginBattle(state);
    return state;
  }

  const player = (s: BattleState, defId: string) =>
    s.units.find((u) => u.side === 'player' && u.defId === defId)!;
  const enemy = (s: BattleState) => s.units.find((u) => u.side === 'enemy')!;

  it('命中で攻撃側に hitXp が入る', () => {
    const s = battle();
    const roran = player(s, 'roran');
    const before = roran.xp;
    s.events = [{
      type: 'hit', targetUid: enemy(s).uid, targetPos: { x: 0, y: 0 }, amount: 3,
      sourceUid: roran.uid, sourceDefId: 'roran', attackKind: 'melee',
      sourcePos: { x: 0, y: 0 }, neraiuchi: false,
    }];
    awardXpForEvents(s);
    expect(roran.xp).toBe(before + s.reg.growth.hitXp);
  });

  it('敵の命中では敵に経験値が入らない', () => {
    const s = battle();
    const e = enemy(s);
    s.events = [{
      type: 'hit', targetUid: player(s, 'roran').uid, targetPos: { x: 0, y: 0 }, amount: 3,
      sourceUid: e.uid, sourceDefId: e.defId, attackKind: 'melee',
      sourcePos: { x: 0, y: 0 }, neraiuchi: false,
    }];
    awardXpForEvents(s);
    expect(e.xp).toBe(0);
  });

  it('回復で術者に healXp が入る', () => {
    const s = battle();
    const mist = player(s, 'mist');
    s.events = [{
      type: 'heal', targetPos: { x: 0, y: 0 }, amount: 5,
      sourceUid: mist.uid, sourceDefId: 'mist', sourcePos: { x: 0, y: 0 },
    }];
    awardXpForEvents(s);
    expect(mist.xp).toBe(s.reg.growth.healXp);
  });

  it('撃破でとどめ役に全額、ダメージを与えた他の味方に半額が入る', () => {
    const s = battle();
    const roran = player(s, 'roran');
    const ines = player(s, 'ines');
    const e = enemy(s);
    const reward = s.reg.enemies.get(e.defId)!.xpReward;
    e.damagedBy = [roran.uid, ines.uid];
    s.events = [{
      type: 'unitDefeated', uid: e.uid, defId: e.defId, byUid: roran.uid, byDefId: 'roran',
      neraiuchi: false, pos: { x: 0, y: 0 },
    }];
    awardXpForEvents(s);
    const assist = Math.floor(reward * s.reg.growth.assistRatio);
    // レベルが上がると xp が繰り越しで減るので、レベルと xp の両方から総量を見る
    expect(totalXp(s.reg, roran)).toBe(reward);
    expect(totalXp(s.reg, ines)).toBe(assist);
  });

  it('とどめ役はアシストぶんを二重取りしない', () => {
    const s = battle();
    const roran = player(s, 'roran');
    const e = enemy(s);
    const reward = s.reg.enemies.get(e.defId)!.xpReward;
    e.damagedBy = [roran.uid];
    s.events = [{
      type: 'unitDefeated', uid: e.uid, defId: e.defId, byUid: roran.uid, byDefId: 'roran',
      neraiuchi: false, pos: { x: 0, y: 0 },
    }];
    awardXpForEvents(s);
    expect(totalXp(s.reg, roran)).toBe(reward);
  });
});

/** レベルアップで繰り越した経験値を足し戻し、累計で何 xp 入ったかを出す */
function totalXp(reg: Registry, unit: Unit): number {
  let total = unit.xp;
  for (let lv = 1; lv < unit.level; lv++) total += xpToNext(lv, reg.growth.xpPerLevel);
  return total;
}
```

`src/ui/flow.test.ts` に追記する。

```ts
describe('applyStageClear: クリアボーナス', () => {
  it('退場していない味方に clearXp が入る', () => {
    const reg = testRegistry();
    const state = freshBattle(reg);        // 既存のヘルパに合わせる
    const roran = state.units.find((u) => u.defId === 'roran')!;
    const r = applyStageClear(reg, newSave(reg), state.stage.id, state);
    expect(r.save.units.roran!.xp).toBe(reg.growth.clearXp);
    expect(roran.retired).toBe(false);
  });

  it('退場した味方には入らない', () => {
    const reg = testRegistry();
    const state = freshBattle(reg);
    const gau = state.units.find((u) => u.defId === 'gau')!;
    gau.retired = true;
    const r = applyStageClear(reg, newSave(reg), state.stage.id, state);
    expect(r.save.units.gau!.xp).toBe(0);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/core/growth.test.ts src/ui/flow.test.ts`
Expected: FAIL。`awardXpForEvents` が無く、`damagedBy` も無い

- [ ] **Step 3: `damagedBy` を足す**

`src/core/types.ts` の `Unit` の `lastHitBy` の近くに足す。

```ts
  /** このユニットにダメージを与えた相手の uid（重複なし）。撃破時のアシスト配分に使う */
  damagedBy: string[];
```

`src/core/state.ts` の `makeUnit` の戻り値に `damagedBy: [],` を足す（`lastHitBy: null` の隣）。

`src/core/damage.ts` の `applyDamage` の `target.lastHitBy = source.uid;` の直後に足す。

```ts
  if (!target.damagedBy.includes(source.uid)) target.damagedBy.push(source.uid);
```

`src/core/skills.ts` の `kakenukeru` も `enemy.lastHitBy = self.uid;` の直後に同じ1行を足す（`applyDamage` を通らず直接 HP を引いているため）。

```ts
      if (!enemy.damagedBy.includes(self.uid)) enemy.damagedBy.push(self.uid);
```

- [ ] **Step 4: 経験値の配分を書き換える**

`src/core/growth.ts` の `awardXpForDefeats` を次に差し替える。

```ts
/** 味方にだけ経験値を入れる。敵は育たない */
function awardXpTo(state: BattleState, uid: string, amount: number): void {
  if (amount <= 0) return;
  const unit = state.units.find((u) => u.uid === uid);
  if (!unit || unit.side !== 'player') return;
  awardXp(state, unit, amount);
}

/**
 * その tick のイベントを見て経験値を配る。
 * 命中・回復・撃破の3つが入り口で、撃破はとどめ役に全額、
 * ダメージを与えた他の味方に assistRatio ぶんを配る
 */
export function awardXpForEvents(state: BattleState): void {
  const { hitXp, healXp, assistRatio } = state.reg.growth;
  // 走査中に events へ levelUp が積まれるので、先にコピーを取る
  const events = [...state.events];
  for (const ev of events) {
    if (ev.type === 'hit') {
      awardXpTo(state, ev.sourceUid, hitXp);
    } else if (ev.type === 'heal') {
      awardXpTo(state, ev.sourceUid, healXp);
    } else if (ev.type === 'unitDefeated') {
      const reward = state.reg.enemies.get(ev.defId)?.xpReward ?? 0;
      if (ev.byUid !== null) awardXpTo(state, ev.byUid, reward);
      const victim = state.units.find((u) => u.uid === ev.uid);
      const assist = Math.floor(reward * assistRatio);
      for (const uid of victim?.damagedBy ?? []) {
        // とどめ役は全額を受け取っているので二重取りさせない
        if (uid === ev.byUid) continue;
        awardXpTo(state, uid, assist);
      }
    }
  }
}
```

`src/core/sim.ts` の `awardXpForDefeats(state);` を `awardXpForEvents(state);` に、import も同様に直す。

- [ ] **Step 5: クリアボーナスを入れる**

`src/ui/flow.ts` の `applyStageClear` のループを次に差し替える。

```ts
  // 経験値はステージ中に確定済み。ここでやるのは確定した進行の書き戻しと、
  // 退場していない仲間へのクリアボーナス
  for (const u of battle.units) {
    if (u.side !== 'player') continue;
    const before = save.units[u.defId] ?? { level: 1, xp: 0 };
    const after = u.retired
      ? { level: u.level, xp: u.xp }
      : applyXp({ level: u.level, xp: u.xp }, reg.growth.clearXp, reg.growth);
    units[u.defId] = after;
    gains.push({ id: u.defId, before, after, leveledUp: after.level > before.level });
  }
```

import に `import { applyXp, earnedTitles } from '../core/progress';` を足す。

- [ ] **Step 6: 撃破経験値を上げる**

| ファイル | `xpReward` |
|---|---|
| `assets/enemies/narazumono.json` | `5` → `8` |
| `assets/enemies/tatemochi.json` | `8` → `12` |
| `assets/enemies/garum.json` | `20` → `30` |

- [ ] **Step 7: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功

- [ ] **Step 8: コミット**

```bash
git add src/core assets/enemies src/ui/flow.ts src/ui/flow.test.ts
git commit -m "feat: 命中・回復・アシスト・クリアボーナスから経験値が入るようにする"
```

---

### Task 11: 画面の文字を漢字仮名交じりにする（項目1）

**Files:**
- Modify: `src/ui/screens.ts`、`src/main.ts:60`、`index.html`（`<title>`）
- Modify: `assets/lines/common.json`
- Modify: `assets/stages/stage1.json`、`assets/stages/stage2.json`、`assets/stages/stage3.json`（`name` / `intro` / `outro`）
- Modify: `assets/skills.json`、`assets/titles.json`
- Modify: `assets/units/*.json`、`assets/enemies/*.json`（`name` / `role`）

**Interfaces:**
- Consumes: なし（表示文字列の差し替えのみ）
- Produces: なし

ルビは付けない。常用漢字を中心に、低学年でも読める語を選ぶ。

- [ ] **Step 1: `src/ui/screens.ts` の文字列を差し替える**

| 現在 | 変更後 |
|---|---|
| `とりでの なかまたち` | `砦の 仲間たち` |
| `4にんの なかまで` | `4人の 仲間で` |
| `てきの ほんきょちへ せめこもう` | `敵の 本拠地へ 攻めこもう` |
| `はじめから` | `最初から` |
| `つづきから` | `続きから` |
| `どの ステージに いく？` | `どの ステージに 行く？` |
| `まだ いけない` | `まだ 行けない` |
| `クリア ずみ` | `クリア済み` |
| `きいろい せんより したに なかまを おこう` | `黄色い 線より 下に 仲間を 置こう` |
| `はじめる` | `始める` |
| `たいきゃく` | `倒れた` |
| `とばす` | `飛ばす` |
| `タップで つぎへ` | `タップで 次へ` |
| `てきの ほんきょちに とうたつ！` | `敵の 本拠地に 到達！` |
| `しょうごう ゲット: ` | `称号 ゲット: ` |
| `つぎへ` | `次へ` |
| `なかまを まもれなかった` | `仲間を 守れなかった` |
| `もういちど` | `もう一度` |
| `しまを えらぶ` | `ステージを 選ぶ` |
| `データの よみこみに しっぱいしました` | `データの 読み込みに 失敗しました` |
| `ほか ${errors.length - 20} けん` | `ほか ${errors.length - 20} 件` |

- [ ] **Step 2: `main.ts` と `index.html` を直す**

`src/main.ts:60` の `assets の よみこみに しっぱい: ${...} けん` を `assets の 読み込みに 失敗: ${...} 件` にする。

`index.html` の `<title>とりでの なかまたち</title>` を `<title>砦の 仲間たち</title>` にする。

- [ ] **Step 3: `assets/skills.json` と `assets/titles.json` の label を直す**

| id | 現在 | 変更後 |
|---|---|---|
| `funbaru` | `ふんばる` | `踏ん張る` |
| `neraiuchi` | `ねらいうち` | `狙い撃ち` |
| `omajinai` | `おまじない` | `おまじない`（変更なし） |
| `kakenukeru` | `かけぬける` | `駆け抜ける` |
| `gamanzuyoi` | `がまんづよい` | `我慢強い` |
| `ichigekihissatsu` | `いちげきひっさつ` | `一撃必殺` |
| `minnanookaasan` | `みんなの おかあさん` | `みんなの お母さん` |
| `kazenoyouni` | `かぜの ように` | `風の ように` |
| `nakayoshi` | `なかよし` | `仲良し` |

- [ ] **Step 4: ユニットと敵の `role` を直す**

| ファイル | 現在 | 変更後 |
|---|---|---|
| `assets/units/roran.json` | `たて` | `盾` |
| `assets/units/ines.json` | `ゆみ` | `弓` |
| `assets/units/mist.json` | `まほう` | `魔法` |
| `assets/units/gau.json` | `ものみ` | `物見` |
| `assets/enemies/*.json`（3件） | `てき` | `敵` |

`name` はすべてカタカナなので変更しない。

- [ ] **Step 5: ステージの `name` と会話を直す**

| 場所 | 現在 | 変更後 |
|---|---|---|
| stage1 `name` | `はじまりの みち` | `始まりの 道` |
| stage2 `name` | `みはりの とりで` | `見張りの 砦` |
| stage3 `name` | `ガルムの さいご` | `ガルムの 最後` |
| stage1 intro[0] | `みちの さきに、けむりが みえる。` | `道の 先に、煙が 見える。` |
| stage1 intro[1] | `みんな、いくよ。\nまえに すすもう` | `みんな、行くよ。\n前に 進もう` |
| stage1 outro[0] | `けむりは もう みえない。` | `煙は もう 見えない。` |
| stage1 outro[1] | `ここまで これた。\nつぎへ いこう` | `ここまで 来れた。\n次へ 行こう` |
| stage2 outro[0] | `とりでを ぬけたぞ！` | `砦を 抜けたぞ！` |
| stage2 outro[1] | `みはりは もう いない。\nさきを いそぎましょう` | `見張りは もう いない。\n先を 急ぎましょう` |
| stage3 outro[0] | `……まさか、\nきみたちに やぶれるとは` | `……まさか、\n君たちに 敗れるとは` |
| stage3 outro[1] | `もう だれのことも\nおびやかさないで` | `もう 誰のことも\n脅かさないで` |
| stage3 outro[2] | `とりでに あさひが さした。` | `砦に 朝日が 差した。` |

- [ ] **Step 6: `assets/lines/common.json` を差し替える**

```json
{
  "first:roran:narazumono": "危ないよ！\nみんな 下がって！",
  "first:roran:tatemochi": "大きな 盾だ……\n僕が 受け止める",
  "first:ines:narazumono": "弱そう。\nさっさと 片付けるわ",
  "first:ines:tatemochi": "うっ、 矢が 弾かれる！\n誰か 前に 出て！",
  "first:mist:narazumono": "こんにちは……\nあ、 敵でしたね",
  "first:mist:tatemochi": "その 盾、\n重くないですか？",
  "first:mist:garum": "わあ、 大きい人",
  "first:gau:narazumono": "よーし、\nいっちょ やるか！",
  "first:gau:tatemochi": "硬い！\n回り込めば いける？",
  "first:gau:garum": "うわ、\n強そうなのが 来た！",

  "rival:roran:garum": "ガルム……\nまた 来たんだね",
  "rival:ines:garum": "また あんたか！\n今度こそ 追い返す",

  "skill:roran": "ここは 通さない！",
  "skill:ines": "動かないで……\n今っ！",
  "skill:mist": "元気に なあれ……",
  "skill:gau": "そこ どいて どいて どいてー！",

  "levelup:roran": "強く なった 気が する！",
  "levelup:ines": "腕が 上がったわね",
  "levelup:mist": "少し 自信が つきました",
  "levelup:gau": "うおー！\nもっと いける！",

  "pinch:roran": "まだ……\n立てる……！",
  "pinch:ines": "ちょっと まずいかも",
  "pinch:mist": "目が 回りますう",
  "pinch:gau": "いたた！\nちょっと 休ませて！",

  "win:roran": "帰って もらったよ",
  "win:ines": "ふん。\n逃げ足だけは 速いのね",
  "win:mist": "行って しまいましたね",
  "win:gau": "やった！\n追い返したぞ！",

  "retire:roran": "ごめん……\n後は 頼む",
  "retire:ines": "くっ……\nここまでね",
  "retire:mist": "ちょっと……\n休みますね……",
  "retire:gau": "うう、\nまだ やれるのに……",

  "stage:stage1:roran": "みんな、\n行くよ。 前に 進もう",
  "stage:stage1:gau": "任せて！\n先に 行ってみる！",
  "stage:stage3:garum": "ここから 先は\n通さんぞ",
  "stage:stage3:roran": "今度は 僕たちが\n攻める 番だ"
}
```

- [ ] **Step 7: テストとビルドを通す**

Run: `npm test && npm run build`
Expected: どちらも成功。ひらがなの文字列をテストが直接参照していれば落ちるので、落ちたテストの期待値を新しい文字列に直す

- [ ] **Step 8: コミット**

```bash
git add src assets index.html
git commit -m "feat: 画面に出す文字を総ひらがなから漢字仮名交じりに変える"
```

---

### Task 12: 正典（README.md / CLAUDE.md）を更新する

**Files:**
- Modify: `CLAUDE.md`
- Modify: `README.md`

**Interfaces:**
- Consumes: Task 1〜11 の変更結果
- Produces: なし

- [ ] **Step 1: `CLAUDE.md` の表記方針を差し替える**

「コミットメッセージ」節の3つ目の箇条書きを次に差し替える。

```markdown
- ゲーム内 UI の表示文字列は**漢字仮名交じり(常用漢字中心、ルビなし)で書く**。総ひらがなに統一しない。低学年でも読める語を選ぶ
```

2つ目の箇条書き（コミットメッセージは漢字仮名交じり）はそのまま残す。

- [ ] **Step 2: `README.md` の「そうさ」節を直す**

変更点は次の5つ。

1. 操作表の「下のポートレートをタップ | 同じく選択・解除」を「下のポートレートをタップ | そのなかまを選び、必殺技を出す」に直す
2. 「必殺技ボタンは下パネルの固定位置に常に出ている。…」の段落を、セリフ欄とポートレート発動の説明に差し替える

```markdown
必殺技は下のポートレートをタップして出す。タップすると同時にそのなかまが選択される。クールダウン中・必殺技を持たない・倒れている場合は選択だけになる。いま出せるなかまのポートレートには明るい縁が付き、残り時間はポートレートのゲージで読める。配置フェーズでは同じ下パネルの上段に「始める」が出る。
```

3. 「戦闘中の会話は、喋ったキャラの頭上に数秒だけ出る。…」の段落を差し替える

```markdown
戦闘中のセリフは下パネルの上段に数秒だけ出る。時間は止まらない。同じ瞬間に複数のなかまが喋ったときは、優先度の高い1件だけを出す。盤面にはかぶらない。
```

4. 「配置中は、ドラッグ先が配置できないマス(壁など)だと…」を、境界線方式に合わせて直す

```markdown
配置は黄色い線より下の、歩けるマスのどこにでもできる。ドラッグ先が線より上か壁だと、プレビューの線と丸が赤く変わり、そのまま離しても失敗することが事前にわかる。
```

5. 索敵範囲についての記述があれば、「敵の索敵範囲は表示しない。気づかれた敵の頭上に赤い『！』が出る」に直す

- [ ] **Step 3: `README.md` の「構成」表を直す**

- `src/ui/skillbutton.ts` の行を削除する
- 次の2行を足す

```markdown
| `src/ui/speech.ts` | 戦闘中のセリフの保持と寿命 |
| `src/core/spawns.ts` | 時間湧きの判定（純関数） |
```

- [ ] **Step 4: `README.md` の「コンテンツの足しかた」を直す**

- ステージの項から `placementZone` の記述を外し、次に差し替える

```markdown
  マップは **16列 × 23行、`cell` は 32**。`placement.minY` より下(画面で下)の歩けるマスが配置できる範囲で、`placement.starts` に開始時の立ち位置を並べる(roster より少なければ先頭から繰り返す)。`victory.pos` は最上段に置く(下から上へ攻める)
```

- 新しい項目を足す

```markdown
- **時間湧きの敵** — ステージの `spawners` に `{ "defId": ..., "pos": ..., "firstAfter": 20, "every": 15, "total": 3 }` を並べる。`firstAfter` 秒後に1体目、以降 `every` 秒ごとに1体、合計 `total` 体まで湧く。湧いた敵の AI は `aggressive` 固定。総数上限は必須(上限がないと持久戦で詰む)
- **成長の調整** — `assets/growth.json`。`xpPerLevel`(レベルアップの必要量)、`hpPerLevel` と `levelsPerPower`(1レベルの強化量)、`hitXp` / `healXp` / `assistRatio` / `clearXp`(経験値の入り口)、`maxLevel`。経験値が小数にならないよう `hitXp` / `healXp` / `clearXp` は整数で検証する
```

- [ ] **Step 5: `README.md` の「描画と入力をブラウザで確認する」を直す**

- 「論理座標 540×945 からクライアント座標への変換は `computeViewport`(`src/render/viewport.ts`)と同じ式を使う」を、`fitCanvas` も併記する形に直す
- 「吹き出しのパネルは `#f7f3e6`」を「セリフ欄のパネルは `#f7f3e6`」に直す
- 「確認しにくいもの」から「同じキャラの連続発話で上書き」「画面端での吹き出しのはみ出し」の記述を外し、`src/ui/layout.test.ts` のクランプ試験への言及も外す(吹き出しを廃止したため)

- [ ] **Step 6: コミット**

```bash
git add README.md CLAUDE.md
git commit -m "docs: issue#11の変更をREADMEとCLAUDE.mdに反映する"
```

---

### Task 13: ブラウザでの目視確認

**Files:**
- 変更なし（確認のみ。問題が出たら該当タスクのファイルを直す）

**Interfaces:**
- Consumes: Task 1〜12 のすべて
- Produces: なし

README.md の CDP 手順を使う。Playwright パッケージは不要で、`~/.cache/ms-playwright/` の Chromium バイナリがあればよい。

- [ ] **Step 1: ビルドして配信する**

```bash
npm run build
npx --yes http-server out -p 8080 &
```

- [ ] **Step 2: 大きいウィンドウでクリック位置を確認する（項目4の回帰の本命）**

```bash
chrome --headless=new --no-sandbox --disable-gpu \
       --remote-debugging-port=9222 --window-size=1600,1200 \
       http://127.0.0.1:8080/play/character-tactics/
```

`http://127.0.0.1:9222/json/list` から WebSocket に繋ぎ、`Runtime.evaluate` で `document.getElementById('game')` へ `PointerEvent` を直接 dispatch する。`Input.dispatchMouseEvent` は headless Chromium で `pointerdown` / `pointerup` として正しく届かないことがあるため使わない。

確認すること。

- [ ] canvas の `getBoundingClientRect()` の縦横比が 540/945 に一致する（1px 未満の差に収まる）
- [ ] タイトルの「最初から」ボタンの論理座標中心（`BTN.titleNew` の中心 = (270, 558)）をクリックしてステージ選択へ進む
- [ ] ステージ選択で `stageSlot(0)` の中心（(140, 220)）をクリックしてステージ1が始まる
- [ ] 配置フェーズでマップ上のなかまの位置をクリックして、そのなかまが選択される（白い破線の輪が出る）

論理座標からクライアント座標への変換は `fitCanvas` と `computeViewport` と同じ式を使う。

- [ ] **Step 3: 配置フェーズを確認する**

- [ ] 黄色い破線が1本、横に引かれ、その下が薄く明るい
- [ ] 線より下の歩けるマスへドラッグして離すと、そこへ置ける
- [ ] 線より上へドラッグするとプレビューの線と丸が赤くなり、離しても動かない
- [ ] 「始める」ボタンが明るい色（`#ffd479`）で、押せるように見える

- [ ] **Step 4: 戦闘中の下パネルを確認する**

- [ ] ポートレートに `Lv1` が出ている
- [ ] 必殺技が出せるポートレートには明るい縁が付いている
- [ ] ポートレートをタップすると必殺技が出る（ロラン=踏ん張る、イネス=狙い撃ち、ミスト=おまじない）
- [ ] ガウのポートレートをタップしたあとマップをタップすると、その方向へ駆け抜ける
- [ ] クールダウン中のポートレートをタップしても技が出ず、選択だけされる

- [ ] **Step 5: セリフ欄を確認する**

- [ ] 敵と初めて交戦したときのセリフが下パネル上段に出る（顔＋本文）
- [ ] 2行のセリフ（`\n` を含むもの）が2行で収まり、枠からはみ出さない
- [ ] 数秒で消える
- [ ] 盤面（マップ領域、論理 y 50〜786）にセリフがかぶらない

`getImageData` でセリフ欄のパネル色 `#f7f3e6` の塊を数えると機械的に取れる。

- [ ] **Step 6: 敵の挙動を確認する**

- [ ] 索敵範囲の円がどこにも出ていない
- [ ] 見張り（sentry）に近づくと頭上に赤い「！」が出て、離れると消える
- [ ] ステージ2で 20 秒ほど経つと湧き口に赤い輪が出て敵が増える
- [ ] 敵の移動が以前より遅い（味方で引き撃ちができる）

- [ ] **Step 7: 成長を確認する**

- [ ] ステージ1をクリアしてリザルトでレベルアップが出る
- [ ] ステージ1クリア時点のレベルを記録する（`assets/growth.json` が甘すぎるかどうかの実測）

spec §8.4 の見積りは計算によるもので未実測。実測が想定から大きく外れていたら、`assets/growth.json` の数値だけを直す。コードは変えない。

- [ ] **Step 8: 文字表記を確認する**

- [ ] タイトル・ステージ選択・配置・戦闘・リザルト・敗北の各画面に、ひらがなだけの文言が残っていない
- [ ] 会話フェーズの本文が枠からはみ出していない（漢字化で文字数は減るが、念のため）

- [ ] **Step 9: 実測値と差異をまとめて報告する**

Step 7 で記録したレベルと、Step 2〜8 で見つかった差異を報告する。`assets/growth.json` の調整が必要なら、その変更だけを別コミットにする。

```bash
git add assets/growth.json
git commit -m "chore: 実機で確認した手応えに合わせて成長の調整値を直す"
```

---

## Self-Review

**1. Spec coverage**

| spec の節 | 対応タスク |
|---|---|
| §3 座標とサイズの土台（項目4） | Task 1、Task 13 Step 2 |
| §4.2 下パネルの新構成（項目5・6・7・12） | Task 3、Task 4、Task 5 |
| §4.3 `skillbutton.ts` の削除 | Task 4 Step 5 |
| §4.4 「はじめる」の配色（項目3） | Task 3 Step 5 |
| §5 吹き出しの撤去とセリフの一本化（項目6） | Task 5 |
| §6 配置フェーズ（項目2） | Task 2 |
| §7.1 移動速度（項目8） | Task 6 Step 6 |
| §7.2 時間湧き（項目9） | Task 7 |
| §7.3 索敵範囲の非表示（項目10） | Task 6 |
| §8.1〜8.2 成長の方針とステータスの式 | Task 8、Task 9 |
| §8.3 経験値の源 | Task 10 |
| §8.4 必要経験値 | Task 8、Task 9 |
| §8.5 既存セーブとの互換 | Task 9（`MAX_LEVEL` は `save.ts` で使われていないので変更不要） |
| §8.6 レベル表示（項目12） | Task 3 Step 6 |
| §8.7 レベルアップのセリフ | 対処なし（spec の判断どおり。Task 13 Step 7 で観察する） |
| §9 表記の漢字化（項目1） | Task 11 |
| §9.2 正典の更新 | Task 12 |
| §10 テスト | 各タスクの Step 1、Task 13 |
| §11 実装順序 | Task 1〜12 の並び |

漏れなし。

**2. Placeholder scan**

「TBD」「あとで」「適切に」「同様に」だけで済ませた手順は無い。Task 11 の文字列と Task 6・10 の数値はすべて具体値を表で示した。Task 13 のみコード無しだが、確認項目をチェックボックスで列挙している。

ただし次の2点は実装者の判断に委ねている。意図的な委譲であり、どちらも既存コードを読めば決まる。

- Task 2 Step 1 の `stageRaw` ヘルパ — `src/engine/schema.test.ts` に同等のヘルパが既にあればそれを使う
- Task 10 Step 1 の `freshBattle` — `src/ui/flow.test.ts` の既存ヘルパに合わせる

**3. Type consistency**

- `fitCanvas` / `CanvasFit`（Task 1）→ Task 13 Step 2 で参照。一致
- `PlacementDef` / `canPlaceAt`（Task 2）→ Task 2 Step 6・7 で参照。一致
- `MESSAGE_BAR`（Task 3）→ Task 4・5 で参照。一致
- `SpeechState` / `pushSpeech` / `tickSpeech` / `clearSpeech` / `speechLines` / `drawSpeechBar`（Task 5）→ Task 5 Step 7 の配線と一致
- `alertMarks` / `AlertMark`（Task 6）→ Task 6 Step 5 の `drawAlertMarks` が `pos` と `defId` の両方を使う。一致
- `SpawnerDef` / `dueSpawns` / `makeEnemyUnit` / `spawnCounts` / `enemySpawned`（Task 7）→ Task 7 Step 8 の `runSpawns` と一致
- `GrowthDef` / `Registry.growth`（Task 8）→ Task 9・10 が参照。一致
- `xpToNext(level, xpPerLevel)` / `applyXp(p, gained, growth)` / `statsForLevel(def, level, growth)`（Task 9）→ Task 10 の `applyXp` 呼び出しと Task 9 Step 5 の呼び出し側修正で一致
- `awardXpForEvents`（Task 10）→ Task 10 Step 4 で `sim.ts` の呼び出しも差し替え。`awardXpForDefeats` という旧名は残らない
- `Unit.damagedBy`（Task 10）→ `damage.ts` と `skills.ts` の両方で記録し、`growth.ts` が読む。一致

矛盾なし。
